---
title: "DreamTrajectory: Trajectory-Guided Action Generation with World Model Alignment for Mobile Manipulation"
title_zh: 梦轨迹：用于移动操作的世界模型对齐的轨迹引导动作生成
authors: "Zheng Yang, Wenjie Zhang, Xiangyu Chen, Wenxuan Song, Xianpeng Wang, Yihang Kang, Wen Chen, Lujia Wang, Renjing Xu, Xiaowen Chu"
date: 2026-08-02
pdf: "https://arxiv.org/pdf/2608.01381v1"
tags: ["query:ad"]
score: 9.0
evidence: 通过世界模型对齐的轨迹引导视觉-语言-动作生成，用于移动操作
tldr: "针对移动操作中VLA策略缺少显式运动规划且开环执行的问题，DreamTrajectory提出轨迹引导的动作生成框架：联合预测末端执行器意图轨迹与全身动作块，并借助轻量轨迹世界模型对候选动作进行测试时搜索-预测-评分，以选出与规划轨迹最对齐的动作。在MS-HAB基准上成功率由32.3%升至47.5%，测试时细化后达54.8%；三个真实移动操作任务平均成功率从63.3%升至90.0%。该方法显著改善了接触丰富的铰接物体操作，为语言条件移动操作提供了新思路。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.01381v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1840, \"height\": 559, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01381v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1841, \"height\": 545, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01381v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1841, \"height\": 870, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.01381v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1852, \"height\": 670, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01381v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 841, \"height\": 246, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01381v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1750, \"height\": 339, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01381v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1439, \"height\": 299, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01381v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 863, \"height\": 229, \"label\": \"Table\"}]"
motivation: 现有VLA策略直接预测全身动作块，缺乏显式任务空间轨迹引导，且开环执行导致规划与实际行动存在偏差。
method: DreamTrajectory联合预测末端轨迹与全身动作块，用轨迹世界模型在测试时搜索-预测-评分对齐候选动作。
result: "MS-HAB成功率32.3%→47.5%，测试时细化→54.8%；真实任务63.3%→81.7%→90.0%。"
conclusion: 轨迹引导与测试时对齐有效提升移动操作精度，尤其在接触丰富任务上。
---

## 摘要
移动操作要求机器人在不断变化的视角和接触条件下协调基座与手臂的运动，其动作空间远大于固定基座操作。现有的视觉-语言-动作（VLA）策略存在两个局限：(i) 它们将观察直接映射到全身动作块，在没有显式任务空间运动规划的情况下搜索这个大动作空间，导致基座-手臂协调预测不精确。(ii) 它们开环执行预测的动作块，不检查动作能否实现策略预期的运动，因此控制误差和未建模的接触会累积，造成规划运动与实际运动之间的差距。我们提出了DreamTrajectory，一个用于语言条件移动操作的轨迹引导框架，针对每个局限引入一个组件。针对(i)，DreamTrajectory在单个动作专家中联合预测意图级别的末端执行器轨迹和全身动作块，使轨迹显式引导基座-手臂动作生成，而不是保持隐式。针对(ii)，一个轻量级轨迹世界模型预测候选动作块将导致的轨迹，并通过测试时搜索-预测-评分过程选择与规划轨迹最对齐的候选。在MS-HAB上，轨迹引导将平均成功率从32.3%提高到47.5%，测试时精炼进一步将其提高到54.8%，在接触丰富的关节物体任务上提升最大。在三个真实世界移动操作任务中，相应的平均成功率分别为63.3%、81.7%和90.0%。

## Abstract
Mobile manipulation requires a robot to coordinate base and arm motion under continuously changing viewpoints and contact conditions, within an action space far larger than that of fixed-base manipulation. Existing Vision-Language-Action (VLA) policies are limited in two respects. (i)They map observations directly to whole-body action chunks, searching this large action space without an explicit task-space motion plan, which makes coordinated base--arm prediction imprecise. (ii)They execute the predicted chunk open-loop, without checking whether the actions can realize the motion the policy intended, so control errors and unmodeled contacts accumulate into a gap between planned and realized motion. We present DreamTrajectory, a trajectory-guided framework for language-conditioned mobile manipulation that introduces one component for each limitation. Addressing(i), DreamTrajectory jointly predicts an intention-level end-effector trajectory and a whole-body action chunk in a single action expert, so that the trajectory explicitly guides base--arm action generation instead of remaining implicit. Addressing(ii), a lightweight trajectory world model predicts the trajectory that a candidate action chunk would induce, and a test-time search--predict--score procedure selects the candidate best aligned with the planned trajectory. On MS-HAB, trajectory guidance raises average success from 32.3% to 47.5% and test-time refinement further to 54.8%, with the largest gains on contact-rich articulated-object tasks. On three real-world mobile manipulation tasks, the corresponding average success rates are 63.3%, 81.7%, and 90.0%.

---

## 论文详细总结（自动生成）

# DreamTrajectory 论文详细总结

## 1. 核心问题与整体含义（研究动机与背景）

- **研究背景**：移动操作（Mobile Manipulation）要求机器人基座与机械臂在持续变化的视角和接触条件下协同运动，其**动作空间远大于固定基座操作**，属于典型的全身（whole-body）协同控制问题。近年来，视觉-语言-动作（VLA）策略被广泛用于该类任务。
- **核心问题**：论文指出现有 VLA 策略在移动操作中面临两大局限：
  - **(i) 缺乏显式的任务空间运动规划**：VLA 策略直接将观察映射到全身动作块（whole-body action chunks），在缺乏任务空间运动轨迹引导的情况下直接在大动作空间中搜索，导致**基座-手臂协调预测不精确**。
  - **(ii) 开环执行导致误差累积**：策略以开环方式执行预测出的动作块，不检查实际执行的动作能否实现策略预期的运动轨迹。控制误差与未建模的接触会不断累积，最终造成**规划运动与实际运动之间的差距**。
- **整体意义**：该工作针对上述两个痛点，提出“轨迹引导 + 世界模型对齐”的复合框架，旨在为语言条件移动操作提供一种更精确、更可控的全身动作生成范式，尤其在**接触丰富的铰接物体操作**（如开门、开抽屉等）上有显著改善。

## 2. 方法论：核心思想、关键技术细节与流程

论文提出 **DreamTrajectory**，一个轨迹引导的语言条件移动操作框架，每个局限对应一个专门组件：

- **组件 1——轨迹引导动作生成（针对局限 i）**：
  - 在一个**单一动作专家（single action expert）**中，**联合预测**两样东西：
    1. **意图层面的末端执行器轨迹**（intention-level end-effector trajectory）；
    2. **全身动作块**（whole-body action chunk）。
  - 关键思想是让显式的末端轨迹**显式地引导**基座-手臂动作生成，而不是像传统 VLA 方法那样让轨迹保持隐式。轨迹相当于一种先验的任务空间运动计划，缩小了搜索空间并提升了基座-手臂的协同精度。

- **组件 2——轻量级轨迹世界模型 + 测试时搜索-预测-评分（针对局限 ii）**：
  - 训练一个**轻量级轨迹世界模型**（lightweight trajectory world model），用于预测：给定一个候选动作块，它将导致什么样的末端执行器轨迹。
  - 在**测试时**执行一个 **搜索-预测-评分（search-predict-score）** 流程：
    1. 先生成多个候选动作块；
    2. 利用轨迹世界模型**预测**每个候选动作块会诱导出的轨迹；
    3. 将预测轨迹与策略规划的意图轨迹进行**对齐评分**，选择**最匹配**的候选动作块作为最终执行动作。
  - 这一过程相当于在闭环意义上对动作进行了“测试时精炼”（test-time refinement），弥补了开环执行的不足。

- **整体流程（文字描述）**：
  ```
  语言指令 + 视觉观察
      ↓
  动作专家：联合预测
      ├── 意图级末端执行器轨迹
      └── 候选全身动作块
      ↓
  轨迹世界模型：预测候选动作块对应的轨迹
      ↓
  评分：将预测轨迹与意图轨迹对齐
      ↓
  选择最优候选动作块执行
  ```

## 3. 实验设计：数据集、Benchmark 与对比方法

- **模拟基准**：使用 **MS-HAB**（移动操作 HABitat 系列基准），这是一个移动操作任务的仿真环境，任务覆盖接触丰富的铰接物体操作。
- **真实世界任务**：在**三个真实移动操作任务**上进行了验证（具体任务内容未在摘要中详述，推测涉及铰接物体操作等接触丰富场景）。
- **对比方法**：
  - 论文未在摘要中列出具体的对比基线方法名称，但从实验设置看，核心对比逻辑包括：
    - **有无轨迹引导的对比**（即标准 VLA 直接预测动作块 vs. 轨迹引导动作生成）；
    - **有无测试时精炼的对比**（即仅轨迹引导 vs. 轨迹引导 + 测试时搜索-预测-评分）。
  - 这构成了类似于逐级消融的评估体系，而非仅与外部基线对比。
- **评估指标**：任务平均成功率（average success rate）。

## 4. 资源与算力

- **论文摘要中未明确说明**使用的 GPU 型号、GPU 数量、训练时长、参数量或显存占用等具体算力信息。
- 仅从“轻量级轨迹世界模型”的描述可以推断，世界模型的设计目标是轻量化、以便在测试时进行搜索-预测-评分而不引入过多计算开销，但具体数值无从得知。
- 如需这部分数据，需要查阅论文正文或附录中的实验设置部分。

## 5. 实验数量与充分性

- **实验总量**：
  - 1 个模拟基准（MS-HAB）上的多任务评估；
  - 3 个真实世界移动操作任务；
  - 消融对比至少覆盖 3 个层次：无轨迹引导（32.3%）→ 轨迹引导（47.5%）→ 轨迹引导 + 测试时精炼（54.8%）。
- **充分性评估**：
  - **优点**：同时覆盖了仿真和真实世界，且真实世界任务成功率达到 63.3%→81.7%→90.0%，体现了从仿真到实物的可迁移性；消融实验设计清晰，能清楚地分离每个组件的独立贡献。
  - **不足**：
    - 摘要中未提供与其他外部 SOTA 方法的对比数据，难以判断相对最新水平的优势有多大；
    - 未提及真实世界任务的种类多样性（是否都属同一类接触丰富任务），以及每项任务的评测次数和方差；
    - 消融层次虽清晰，但未展示更细粒度的消融（如世界模型本身的质量评估、搜索候选数量的影响等）；
    - 在判断“是否客观公平”时，由于缺乏外部基线对比细节，其纵向对比充分性需要结合全文进一步核实。

## 6. 主要结论与发现

- **轨迹引导有效**：在 MS-HAB 上，加入轨迹引导后平均成功率从 32.3% 提升至 47.5%，提升约 15.2 个百分点，验证了“显式任务空间轨迹引导基座-手臂动作生成”这一核心假设。
- **测试时精炼进一步增强**：加入轨迹世界模型驱动的测试时搜索-预测-评分后，成功率进一步提升至 54.8%，证明开环执行导致的误差可以通过测试时对齐有效缓解。
- **接触丰富任务受益最大**：论文明确指出在**接触丰富的铰接物体任务**上提升幅度最大，说明轨迹引导和世界模型对齐对于需要精确、持续接触操作的任务尤为关键。
- **真实世界可迁移性好**：三个真实世界移动操作任务的平均成功率从 63.3%（基线）提升至 81.7%（轨迹引导）再到 90.0%（+测试时精炼），表明该方法的增益不仅能停留在仿真中，还能在真实机器人上复现。

## 7. 优点

- **问题定位精准**：明确指出现有 VLA 在移动操作中的“缺乏显式运动规划”和“开环执行”两大结构性缺陷，针对性极强。
- **方法设计优雅且自洽**：
  - 用“意图级轨迹”显式引导动作生成，在不增加额外大模型的情况下利用了任务空间的低维结构；
  - 用“轻量级世界模型”进行测试时对齐，既弥补了开环误差，又保持了推理效率。
- **仿真-真实双验证**：在 MS-HAB 模拟基准和真实世界任务上均取得了显著且一致性的提升，增加了结论的可信度。
- **增益显著且趋势一致**：无论是模拟还是真实场景，轨迹引导和测试时精炼都呈现逐级递进的收益，逻辑自洽。
- **消融层次明确**：通过三档设置（基线 / +轨迹引导 / +测试时精炼）清晰量化了每个组件的独立贡献。

## 8. 不足与局限

- **实验覆盖限制**：
  - 摘要中仅提到 MS-HAB 一个仿真基准，未涉及更多样化的移动操作 benchmark（如 RLBench、CALVIN 等），泛化性验证尚不充分；
  - 真实世界任务只有三个，且任务性质偏向接触丰富型，未体现方法在非接触或动态环境任务上的表现。
- **外部对比不足**：缺少与其他先进 VLA 方法（如 RT-2、OpenVLA、π0 等）在相同基准上的直接对比，无法判断该方法的相对竞争力。
- **评估信息不完整**：未提供任务成功率的方差、评测次数、真实世界任务的具体设置细节（机器人平台、传感器配置等）。
- **算力信息缺失**：未说明训练所需的算力资源配置，难以评估方法的可复现成本。
- **潜在偏差风险**：
  - 测试时搜索-预测-评分的质量高度依赖轨迹世界模型的准确性；如果世界模型本身在分布外场景下失准，评分选择可能引入新的偏差；
  - 轨迹引导可能使策略过度依赖末端轨迹预测，在轨迹预测困难的任务中（如高度动态场景）可能成为瓶颈。
- **应用限制**：
  - 方法以语言条件为输入，依赖语言指令的可解析性；
  - 测试时搜索增加了推理阶段额外计算，在实时性要求极高的场景中需要进一步优化。

（完）
