---
title: Accelerating Human-Aware Robot Trajectory Generation via Diffusion and Consistency Distillation
title_zh: 通过扩散与一致性蒸馏加速人类感知的机器人轨迹生成
authors: "Byeong-Il Ham, Hyun-Bin Kim, Kyung-Soo Kim"
date: 2026-08-04
pdf: "https://arxiv.org/pdf/2608.03159v1"
tags: ["query:ad"]
score: 9.0
evidence: 人机交互，机器人操作臂轨迹生成，RRT与扩散蒸馏
tldr: "在人类-机器人交互场景中，非冗余机械臂的末端位姿由任务确定后，额外的碰撞避免和自碰撞避免需求很难作为零空间次级任务处理，导致关节空间轨迹难以生成。本文利用RRT和RRT*算法生成大量满足约束的轨迹作为训练数据，训练扩散模型以通过引导采样直接生成约束满足的轨迹。为了克服扩散采样迭代耗时的问题，采用一致性蒸馏大幅降低推理延迟，并在损失函数中加入关节加权加加速度正则化项来惩罚加速度突变，使轨迹更平滑。仿真结果显示，一致性模型在不到100毫秒内可以生成150个候选轨迹，同时保持很高的情节成功率，施加正则化后关节和末端加加速度均显著下降。该工作为实时、平滑且安全的人机交互轨迹生成提供了有效的加速方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.03159v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1901, \"height\": 289, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03159v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1907, \"height\": 308, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03159v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 898, \"height\": 736, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03159v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 748, \"height\": 592, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.03159v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 911, \"height\": 359, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03159v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 911, \"height\": 359, \"label\": \"Table\"}]"
motivation: 针对非冗余机械臂在末端位姿确定时，碰撞和自碰撞约束难以作为零空间任务处理的问题。
method: "利用RRT和RRT*生成约束轨迹数据，训练扩散模型，采用一致性蒸馏加速推理，并加入关节加权加加速度正则化。"
result: 模型在100毫秒内生成150个候选轨迹，保持高成功率且显著降低关节和末端加加速度。
conclusion: 提出了一种快速、平滑且安全的HRI轨迹生成方案，在保持成功率的同时实现了实时推理。
---

## 摘要
本研究提出了一种用于人机交互（HRI）环境下机器人操纵器的约束运动规划框架。对于末端执行器位姿完全确定的非冗余操纵器，碰撞避免和自碰撞避免等额外要求难以作为简单的零空间次要任务来处理。这一局限性使得在需要同时考虑安全与运动学约束的人机交互环境中生成可行的关节空间轨迹充满挑战。为解决该问题，利用快速探索随机树（RRT）和RRT*算法生成具有碰撞与自碰撞感知的轨迹，并利用所得数据集训练扩散模型，通过引导采样生成满足约束的轨迹。为减少迭代扩散采样所需的推理时间，采用了一致性蒸馏，并在损失函数中加入关节加权加加速度正则化项，通过惩罚关节加速度的突变来促进生成更平滑的轨迹。仿真结果表明，一致性模型可在不到100毫秒内生成150个轨迹候选，保持较高的回合成功率，并且在应用加加速度正则化时，显著降低了关节和末端执行器的加加速度。

## Abstract
This research proposes a constrained motion planning framework for robot manipulators in human-robot interaction (HRI). For a non-redundant manipulator with a fully specified end-effector pose, additional requirements such as collision avoidance and self-collision avoidance are difficult to handle as simple null-space secondary tasks. This limitation makes it challenging to generate feasible joint-space trajectories in HRI environments where safety and kinematic constraints must be considered simultaneously. To address this limitation, collision- and self-collision-aware trajectories are generated using Rapidly-exploring Random Tree (RRT) and RRT* algorithms, and the resulting dataset is used to train a diffusion model that generates constraint-satisfying trajectories through guided sampling. To reduce the inference time required for iterative diffusion sampling, consistency distillation is applied, and a joint-weighted jerk regularization term is incorporated into the loss function to promote smoother trajectories by penalizing abrupt changes in joint acceleration. Simulation results show that the consistency model generates 150 trajectory candidates in less than 100 ms, maintains a high episode success rate, and substantially reduces joint and end-effector jerk when jerk regularization is applied.

---

## 论文详细总结（自动生成）

## 论文总结：加速人类感知机器人轨迹生成（Diffusion + Consistency Distillation）

### 1. 核心问题与研究动机
- 背景：机器人操纵器正在越来越多地进入与人共享的工作空间，**人机交互（HRI）中的碰撞避免**成为关键安全需求。
- 核心难点：对于**非冗余（6-DoF）机械臂**，当末端执行器（EE）的位姿被完全指定时，碰撞避免、自碰撞避免等额外约束**无法作为零空间次级任务**来处理，使得生成可行且安全的关节空间轨迹非常困难。
- 现有方法的不足：
  - 采样方法（RRT/RRT*）可处理复杂约束但计算开销大，难以满足 HRI 的实时性需求；
  - 优化方法依赖于路径/航点预设，约束增多时计算复杂度上升；
  - 扩散模型可生成多样化轨迹，但迭代式去噪导致**推理时间过长**。
- 研究目标：构建一个**同时满足安全、运动学可行、平滑且快速**的 HRI 关节空间轨迹生成框架。

### 2. 方法论

#### 2.1 整体框架（核心思想）
- 用 **RRT 与 RRT\*** 生成大量满足约束（碰撞避免、自碰撞避免、关节极限）的轨迹数据；
- 利用这些数据训练一个**以任务条件为输入的扩散模型**，通过约束引导采样生成可行轨迹；
- 使用**一致性蒸馏**大幅缩短推理时延（从秒级降至毫秒级）；
- 在损失函数中引入**关节加权加加速度正则化**，提升轨迹平滑度。

#### 2.2 约束规划构建（有限冗余下的任务处理）
- 末端位姿目标（位置 `x_g` + 姿态 `R_g`）通过**数值逆运动学（IK）**转化为关节空间目标；
- 采用**阻尼最小二乘**迭代更新关节估计：
  `Δξ = (JᵀJ + λI)⁻¹ Jᵀ e`
- 在多个 IK 解中，筛选满足碰撞/自碰撞条件的候选，并以**可操作性度量** `m(ξ) = sqrt(det(JJᵀ))` 最大者为目标构型；
- RRT/RRT\* 生成中间路径，经插值重采样为固定长度（T 个航点）的轨迹，形成训练数据集。

#### 2.3 条件扩散模型
- **条件输入 c**：初始关节构型 + 末端目标位姿（用 9 维向量编码：3D 位置 + 旋转矩阵前两列）+ 障碍物胶囊体特征（两端点 + 半径，共 7 维/个）；
- 障碍物特征经 **PointNet 风格集合编码器**（共享 MLP + 最大池化）提取全局特征；
- 主干网络为**时间 U-Net**（残差 1D 卷积块沿航点维度处理）；
- 前向加噪过程：`q_d^k = √ᾱ_k · q + √(1-ᾱ_k) · ε`；
- 训练损失为噪声预测的 MSE（式 8）。

#### 2.4 约束引导采样
- 在反向去噪的每一步中，对预测均值施加**可微约束代价函数（环境碰撞 + 自碰撞）的梯度修正**：
  `μ̃ = μ - η_k · ∇_μ C(μ)`
- 引导模型在保持轨迹分布特性的同时避障，提升不可见场景下的泛化能力。

#### 2.5 一致性蒸馏（推理加速）
- 以已训练的扩散模型为教师网络，蒸馏出一个**一致性模型** `f_θ`，其支持从任意加噪状态直接映射到干净轨迹（式 12）；
- 通过**教师引导的重新加噪**（式 13）构造中间状态，EMA 目标网络提供参考估计；
- 一致性蒸馏损失 `L_CD` 惩罚学生网络与目标网络之间的估计差异（式 14）；
- 推理时仅需 **M 步采样**（如 4 步）即可生成高质量轨迹，总推理时间降至 **~100 ms 以内**。

#### 2.6 关节加权加加速度正则化
- 在一致性训练损失中加入**离散三阶差分**近似关节加加速度（式 15）；
- 采用**三重权重**：低可操作性构型权重更大、关节平移灵敏度（Jacobian 列范数）权重、目标附近航点时间权重；
- 最终损失：`L_consistency = L_CD + λ_jerk · L_jerk`（式 19）。

### 3. 实验设计

| 项目 | 内容 |
|---|---|
| 仿真环境 | UR5 6-DoF 协作机器人 + 末端力/力矩传感器与接触板 |
| 障碍物建模 | 人体躯干、上臂、前臂（橙色）+ 躯干（灰色）+ 目标位姿（绿色） |
| 轨迹数据集 | 共 51,000 条，RRT 与 RRT\* 按 2:8 比例生成 |
| 数据划分 | 训练 42,000 / 验证 5,000 / 测试 500 |
| 对照组 | (a) 扩散模型 w/ vs w/o 加加速度正则化；(b) 一致性模型 w/ vs w/o 加加速度正则化 |
| 评测指标 | 推理时间、任务成功率、可操作性、关节/末端加加速度、末端路径长度 |
| 成功判据 | EE 位置误差 < 5 cm & 姿态误差 < 10°（每个场景生成 150 个候选，取可操作性最优者） |

### 4. 资源与算力
- **GPU**：单张 NVIDIA GeForce RTX 5070 Ti，Ubuntu 24.04，PyTorch 实现；
- **动力学/运动学库**：Pinocchio；
- **论文未明确给出**训练时长、批量大小、训练轮次、参数量等细节。

### 5. 实验数量与充分性
- 扩散推理实验：2 组（w/ vs w/o jerk 正则化）；
- 一致性推理实验：2 组（w/ vs w/o jerk 正则化）；
- **总数不多，但消融设计合理**：同时验证了蒸馏的效果（扩散 vs 一致性）和加加速度正则化的效果（有 vs 无）；
- 客观性方面：使用确定性指标（成功率、加加速度、路径长度、推理时间）且采样规模一致（150 个候选/场景），对比较为公平；
- 不足之处：**未与其他方法（如纯优化法、其他生成式方法）进行直接对比**，且仅限单一机械臂（UR5）和静态人形场景。

### 6. 主要结论与发现
- 扩散模型成功学习人类感知安全轨迹分布，成功率高达 98.8%~99.6%；
- **加加速度正则化**使扩散模型关节加加速度降低 36.89%、末端加加速度降低 36.25%，且不损失成功率；
- **一致性蒸馏**将推理时间从约 5.7 s 降至约 **95 ms**（降低 ≈98%），同时保持高成功率；
- 蒸馏后加加速度略有上升（关节 +9.6%），但加加速度正则化有效抑制了该上升趋势；末端加加速度反而下降 13.7%，综合性能优越。

### 7. 方法亮点
- **问题定位准确**：针对非冗余机械臂在末端位姿确定时次级约束难以处理的切实问题展开研究；
- **数据生成与学习框架结合巧妙**：用采样方法生成数据、用扩散模型学习分布、用引导采样保障安全，各环节衔接自然；
- **加速策略高效**：一致性蒸馏使轨迹生成达到实时水平（<100 ms / 150 条候选）；
- **平滑性设计细致**：三重权重（可操作性、灵敏度、时间）的加加速度惩罚设计合理，在关节空间中显式地抑制了运动突变；
- **实验指标相对全面**：覆盖了任务成功率、平滑性（jerk）、路径长度与可操作性等多个维度。

### 8. 不足与局限
- **仅仿真验证**：未在实体机械臂上部署，未考虑轨迹跟踪误差、执行器延迟和建模误差；
- **静态人体假设**：未涵盖动态人体运动、人体意图预测和在线感知更新；
- **缺少感知噪声建模**：障碍物表示假设为理想胶囊体，未考虑视觉/深度传感器的测量不确定性；
- **对比实验有限**：未与优化法（如 CHOMP/STOMP）、其他扩散类方法（如 ManiCM）或纯 RRT* 方法进行端到端基准比较；
- **算力细节缺失**：未报告训练时间、模型参数量和能耗等实用指标，不利于工程复现评估；
- **场景多样性有限**：单一机械臂类型、固定任务模式（末端靠近人手），可推广性有待验证。

（完）
