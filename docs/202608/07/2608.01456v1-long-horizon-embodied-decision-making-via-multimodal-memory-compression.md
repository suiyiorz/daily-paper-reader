---
title: Long-Horizon Embodied Decision-Making via Multimodal Memory Compression
title_zh: 长时程具身决策中的多模态记忆压缩
authors: "Bingxuan Li, Rui Yang, Cheng Qian, Jiateng Liu, Jeonghwan Kim, Zhenhailong Wang, Manling Li, Tong Zhang, Heng Ji"
date: 2026-08-02
pdf: "https://arxiv.org/pdf/2608.01456v1"
tags: ["query:ad"]
score: 8.0
evidence: 物理住宅环境中的具身决策基准
tldr: "长程具身决策要求智能体在部分观测下综合多模态信息并推断用户偏好，现有方法在复杂住宅环境中表现欠佳。为此提出DunphyBench基准，系统评估发现当前视觉语言模型驱动智能体与人类差距悬殊，其中记忆管理是关键瓶颈。进一步设计MeMento，基于用户偏好对长程历史进行条件化压缩，仅用固定记忆token保留决策相关信息。在基准上MeMento将准确率提升7.18%，同时减少85.38%内存占用。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.01456v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 721, \"height\": 303, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01456v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1438, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01456v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1443, \"height\": 579, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01456v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1432, \"height\": 767, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01456v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 759, \"height\": 423, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.01456v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1360, \"height\": 362, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01456v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1381, \"height\": 903, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01456v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 623, \"height\": 387, \"label\": \"Table\"}]"
motivation: 长程具身决策需要长期理解与偏好推断，现有智能体因记忆噪声导致决策质量差，与人类差距明显。
method: 提出MeMento，用偏好条件化的记忆压缩器，将多模态历史选择性压缩为固定数量记忆token。
result: "在DunphyBench上，MeMento提升VLM智能体准确率7.18%，降低内存85.38%。"
conclusion: 记忆压缩可有效缓解长程决策中的噪声干扰，为具身智能体高效记忆管理提供新思路。
---

## 摘要
智能体越来越被期望不仅作为任务执行者，还要代表人类用户进行决策。这种转变要求智能体在长时程内积累证据，理解隐式的用户偏好，并在部分观测条件下比较多个候选方案。在这项工作中，我们提出了DunphyBench，一个新的基准，用于评估智能体在长时程、以人为中心的具身决策中的表现，其中智能体必须导航多个具身居住环境，并做出符合多维人类偏好的决策。与通常关注程序性规划或即时目标完成的标准化具身推理任务不同，我们的设置要求智能体将多模态、多来源的输入整合为连贯的知识，以支持跨长时程的复杂推理。评估结果显示，当前智能体与人类表现之间存在显著差距。此外，我们对最先进的VLM驱动智能体的诊断表明，记忆管理是瓶颈之一，其中原始多模态历史引入了干扰决策质量的噪声。受此发现启发，我们设计了MeMento，一种偏好条件化的多模态记忆压缩器，它基于用户偏好，使用固定数量的记忆标记，从长时程历史中选择性地压缩与决策相关的信息。实验表明，与最强基线相比，MeMento帮助VLM驱动的智能体将准确率提高了7.18%，同时将记忆使用量减少了85.38%。

## 速览
**TLDR**：长时程具身决策要求智能体在部分可观察环境中整合多模态信息并推断隐性偏好，现有智能体与人类差距显著。作者提出DunphyBench基准和MeMento方法，后者基于偏好压缩多模态记忆，以固定token选择性保留关键信息，提升准确率7.18%并减少85.38%内存占用。 \
**Motivation**：现有基准侧重程序性规划，缺乏长时程多模态整合与偏好推断的评估，导致智能体决策能力与人类存在明显差距。 \
**Method**：MeMento利用偏好条件化的多模态记忆压缩器，将长时程历史中的决策相关信息压缩为固定数量记忆token，供VLM驱动智能体使用。 \
**Result**：在DunphyBench上，MeMento使VLM智能体准确率提升7.18%，内存占用较最强基线减少85.38%。 \
**Conclusion**：记忆管理是长时程具身决策的瓶颈，偏好引导的压缩策略可显著提升决策质量与效率。

---

## Abstract
Agents are increasingly expected to act not only as task executors, but also as decision-makers on behalf of human users. This shift requires agents to accumulate evidence over long horizons, interpret implicit user preferences, and compare multiple candidates under partial observations. In this work, we propose DunphyBench, a new benchmark for evaluating agents on long-horizon human-centered embodied decision-making, where the agent must navigate through multiple embodied housing environments and make decisions that align with multi-dimensional human preferences. Unlike standard embodied reasoning tasks that often focus on procedural planning or immediate goal completion, our setting requires agents to integrate multimodal, multi-source input into coherent knowledge that supports complex reasoning across long horizon. The evaluation results reveal that there is a substantial gap between current agents and human performance. Furthermore, our diagnosis of state-of-the-art VLM-driven agents reveals that memory management is one of the bottlenecks, where raw multimodal history introduces noise that hinders decision quality. Motivated by this finding, we design MeMento, a preference-conditioned multimodal memory compressor that selectively compresses decision-relevant information from long-horizon history based on user preferences with a fixed set of memory tokens. Experiments show that MeMento helps VLM-driven agents improve accuracy by 7.18%, while reducing memory usage by 85.38% compared to the strongest baseline.

---

## 论文详细总结（自动生成）

## 论文详细总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：智能体正从单纯的任务执行者向代表人类用户的决策者转变。真实世界的长时程决策（如找房子）要求智能体在长时间内积累证据、理解隐含的用户偏好，并在部分可观察条件下对多个候选方案进行系统比较。人类自身在长时程决策中也会因信息过载而产生"决策疲劳"（decision fatigue），因此需要一个能够保持长时程记忆、系统比较候选方案并代表用户进行深思熟虑决策的智能助手。
- **核心问题**：当前VLM驱动的智能体能否在长时程、偏好驱动的具身交互中做出符合用户偏好的决策？论文围绕两个核心挑战展开：
  - **偏好欠指定（preference under-specification）**：用户偏好并非总是以明确的检查清单形式给出，可能是隐式、间接或人格驱动的（如"我经常为朋友做饭"），需要智能体从描述性信号中推断具体需求。
  - **长时程接地（long-horizon grounding）**：智能体需将个性化的偏好信号与积累的具身探索经验相连接，在不同候选环境之间综合比较证据。
- **整体含义**：现有基准多聚焦于短时程的程序性规划或即时目标完成，缺乏对长时程多模态信息整合与偏好推断能力的系统评估。论文填补了这一空白，揭示了当前智能体与人类在长时程具身决策上的显著差距，并指出记忆管理是其中一个关键瓶颈。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

#### 2.1 DunphyBench基准构建

- **任务设定**：基于房屋选择任务，每个实例包含N条用户偏好（N=7）和一个候选池（1+3M个环境），其中1个是满足全部偏好的黄金环境，其余3M个为仅部分满足的干扰项，分为三个难度层级（easy/medium/hard），每层M个环境。
- **环境表示**：基于ProcTHOR程序化生成的具身住宅环境，将每个环境转化为结构化特征向量（房间数、布局、房间属性、物体清单、空间连通性、美学特征），并定义确定性谓词作为自然语言偏好与环境验证之间的接口。
- **偏好构造**：设计了两个独立评估轨道——**显式轨道**（偏好直接指定为结构化约束，一对一映射到可验证谓词）和**隐式轨道**（偏好以自然语言陈述形式给出，如"我经常做大餐"，需要模型推断潜在的结构性需求）。
- **偏好分类**：覆盖7个语义类别：房间数量与类型、房间尺寸与面积、窗户与采光、物体存在性、物体数量、空间连通性、材料与装饰。
- **数据生成流水线**：特征提取→黄金环境采样→偏好生成（LLM配合谓词映射）→干扰项构建（按偏好满足度分区控制难度）→质量控制（确保黄金满足所有偏好、干扰项均不满足、跨实例冗余过滤等）。

#### 2.2 MeMento记忆压缩方法

- **核心思想**：将多模态历史压缩为固定数量的偏好相关记忆token，避免原始历史中的噪声信息干扰决策。整体架构为**偏好条件化的Perceiver式模块**。
- **偏好编码**：将用户需求（显式约束或隐式陈述）通过冻结的VLM骨干转换为关键词，提取关键词文本嵌入作为偏好token，最终池化为单一偏好嵌入$\bar{z}_r$作为记忆选择的信号。
- **多模态步编码**：每个探索步骤包含自我视角图像、地图视角、观察文本和动作文本，使用多模态编码器$f_{enc}$编码为步表示$e_t$，并加入时间戳位置编码$p^t$和环境身份编码$p^{env}_k$以保留时序和环境信息。
- **偏好条件化记忆压缩**：
  - 学习查询集$Q_0$，通过投影网络$\phi(\cdot)$注入偏好嵌入得到偏好条件化查询：$Q_r = Q_0 + \phi(\bar{z}_r)$
  - 对每个环境$E_k$独立压缩，防止不同候选环境之间的证据混合：
    $H^{(k)} = \text{SelfAttn}(\text{CrossAttn}(Q_r, \{\tilde{e}_t : t \in E_k\}, \{\tilde{e}_t : t \in E_k\}))$
  - 各环境级记忆token拼接形成全局压缩记忆token $H_{global}$
- **压缩记忆注入**：通过可训练适配器$\psi(\cdot)$将压缩token投影到冻结VLM的嵌入空间，以结构化的记忆前缀形式注入到当前步骤的提示token之前：$[\tilde{M}; X_{current}]$
- **两阶段训练**：
  - **阶段1（偏好引导压缩训练）**：添加预测头以预测步级偏好相关性，标注类型为{positive, negative, unknown, irrelevant}，损失函数为$L_{stage1} = L_{rel} + \lambda L_{attn}$，其中$L_{rel}$为交叉熵分类损失，$L_{attn}$为将压缩器交叉注意力权重与相关性标签对齐的损失。
  - **阶段2（对齐训练）**：训练压缩器和投影适配器，使压缩记忆可被冻结的VLM读取。通过偏好相关的QA任务（如"水槽附近有窗户吗？"）优化语言建模损失，VLM骨干保持冻结，梯度通过其计算图传播至可训练适配器和压缩器。
- **推理**：去掉预测头，仅执行编码、压缩和注入流程。

### 3. 实验设计：数据集/场景、基准、对比方法

#### 3.1 基准与数据集

- **主基准**：DunphyBench，默认配置N=7、M=19，即7条用户偏好、58个候选环境。涵盖显式与隐式两个偏好轨道。
- **任务场景**：房屋选择任务——智能体在多个ProcTHOR生成的具身住宅环境中导航探索，最终选出最符合用户偏好的房屋。
- **迁移实验场景**：面向网页代理的100个网页任务（基于Embodied Web Agent环境），任务含明确的约束要求（如价格或位置限制）。

#### 3.2 对比方法

- **主干模型**：Qwen3VL-8B-Instruct、Qwen3VL-30B-Thinking、Gemma-4-26B-A4B-it、GPT-5.4
- **记忆配置变体**：无记忆（直接一次性推理）、文本记忆（存储最近步骤的结构化文本摘要）、多模态记忆（文本+视觉上下文，如自我视角和地图视角图像）
- **工具配置**：Sketchpad（持久化工作区，记录中间想法和观察）、Retriever（基于查询的记忆检索工具），以及两者的组合。
- **MeMento消融**：MeMento_P（移除偏好关键词生成模块，直接编码原始需求为稠密嵌入）、MeMento_T（仅用文本记忆压缩，不含视觉信息）、MeMento_U（移除层次化两阶段设计，单阶段压缩全部记忆）。
- **记忆基线**：Memoryless、Text Recent Memory、Multimodal Recent Memory、Text Summarization Memory、RAG（文本/图像检索）。
- **人类基线**：人类标注者在相同评估协议下进行评测。

#### 3.3 评估指标

- **准确率（Acc）**：最终决策正确的实例比例
- **归一化满足率（nSAT）**：所选环境的满足属性与黄金需求的交集大小，衡量部分正确性
- **探索效率（EE）**：$EE = (1 - K/K_{max}) \times nSAT$，平衡探索步数与决策准确率

### 4. 资源与算力

- **算力信息**：论文**未明确说明**使用的GPU型号、数量、训练时长等算力资源细节。仅能推断其基于Qwen3VL-8B-Instruct进行MeMento的训练和推理实验，但具体硬件配置、训练时间和计算开销均未在提取的文本中交代。

### 5. 实验数量与充分性

- **主实验**：4个模型×3种记忆配置×多种工具配置组合（共约28种配置），每种配置在显式和隐式轨道上分别评估，规模适中。
- **难度分析**：对不同难度层级（Easy/Medium/Hard）和不同参数规模（N=7/21/35，M=4/9/19）进行了系统分析。
- **消融实验**：MeMento的3个变体（去偏好模块、去视觉记忆、去层次压缩）均做了对比。
- **记忆探查实验**：通过记忆QA任务比较MeMento与多个记忆基线的召回细粒度信息能力。
- **迁移实验**：在100个网页代理任务上验证了MeMento的可泛化性。

**充分性评估**：实验设计较为全面，覆盖了模型规模、记忆类型、工具配置、难度层级、偏好类型（显式/隐式）等多个维度，并通过消融和迁移实验验证了方法组件的有效性和通用性。但存在以下客观局限：（1）主基准的实例数量相对有限（约36个测试实例，从表2的百分比可推断），统计显著性可能不足；（2）迁移实验仅覆盖网页任务，未能验证到更多样化的决策场景；（3）基准任务集中于住房选择这一单一应用场景。

### 6. 论文的主要结论与发现

1. **智能体与人类存在显著差距**：最佳智能体准确率为58.3%，远低于人类的83.3%。
2. **任务复杂度导致性能急剧下降**：从Easy到Hard难度，显式准确率从49.1%降至25.0%，隐式从26.7%降至16.1%；干扰项从M=4增加到M=19时，准确率从42.6%降至15.3%。
3. **隐式偏好推理是关键瓶颈**：隐式偏好的性能始终显著低于显式偏好（例如Easy难度下49.1% vs. 26.7%），说明智能体推断欠指定用户意图的能力严重不足。
4. **记忆是性能的重要驱动因素**：增加记忆大幅提升部分模型的准确率（GPT-5.4从25.0%无记忆提升到50.0%文本记忆，再到58.3%多模态记忆），但记忆缩放是非单调的——过多未过滤的上下文会引入噪声并导致性能退化。
5. **工具提供的增益有限且不稳定**：Sketchpad和Retriever等工具仅带来边际改进，有时甚至因上下文开销而产生负面影响。
6. **MeMento的有效性**：在DunphyBench上将VLM智能体准确率提升7.18%（相对提升15.74%），同时较最强基线减少85.38%的内存使用（约60000 token降至约3800 token）。
7. **MeMento的组件协同**：偏好感知表示贡献最大（去掉后准确率从52.78%降至15.67%），多模态记忆和层次化压缩提供互补增益。
8. **可泛化性**：在网页决策任务中，MeMento以512个记忆token达到83%成功率，优于文本摘要（72%成功率、1k token）和最近多模态记忆（69%成功率、4.5k token）等基线。

### 7. 优点

1. **基准设计创新**：DunphyBench是首个系统性评估长时程、以人为中心的具身决策能力的基准，同时具备显式/隐式偏好分离设计、七类偏好语义覆盖、三级可控难度和自动验证机制，避免了标注歧义。
2. **洞察深刻**：通过对SOTA VLM智能体的系统诊断，明确识别出记忆管理是核心瓶颈，并发现记忆缩放的非单调现象——这一发现对设计长时程智能体具有重要指导意义。
3. **方法新颖**：MeMento将偏好条件化与Perceiver式记忆压缩相结合，用固定数量的记忆token选择性保留决策相关信息，理念简洁有效。两阶段训练（先做相关性定位再与VLM空间对齐）设计合理。
4. **层次化压缩设计**：按环境独立压缩再拼接，防止候选环境间证据混合，保留了环境身份信息，符合决策任务的逻辑结构。
5. **评估充分**：多维度消融、记忆探查QA、跨任务迁移验证等实验设计全面，验证了各组件贡献和方法的泛化性。
6. **效率优势明确**：在显著提升准确率的同时大幅降低token使用量，兼顾性能与效率。

### 8. 不足与局限

1. **实验规模有限**：测试实例数相对较少（从百分比数据推算约36个实例），统计功效有限，可能影响结论的可靠性。
2. **任务场景单一**：基准聚焦住房选择任务，虽提及可推广到其他决策任务，但缺乏实证支撑。
3. **算力细节缺失**：未报告训练所需的GPU配置、训练时间和计算成本，影响复现和实际应用评估。
4. **长期记忆的实际效果存疑**：MeMento虽将记忆压缩到3.8K token，但训练和推理过程本身引入的额外计算开销未被讨论。
5. **工具增益有限**：实验显示工具（Sketchpad、Retriever）的组合在某些情况下反而降低性能，未对工具设计本身做更深入优化。
6. **隐式偏好表达方式有限**：隐式偏好语句限制在20词以内，真实场景中用户的需求描述可能更复杂多变。
7. **依赖VLM骨干的偏好理解能力**：偏好关键词生成和QA对齐训练依赖所选的Qwen3VL-8B骨干，不同骨干（如GPT-5.4）的性能差异可能导致MeMento的泛化效果在不同模型上不一致。
8. **评估指标有待完善**：性能下降的临界记忆预算（turns数在15左右）报告得较粗略，对不同干扰类型、不同记忆内容质量的细粒度分析不足。

（完）
