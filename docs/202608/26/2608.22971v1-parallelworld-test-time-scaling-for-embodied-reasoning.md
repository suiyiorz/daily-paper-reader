---
title: "ParallelWorld: Test-Time Scaling for Embodied Reasoning"
title_zh: 平行世界：具身推理的测试时缩放
authors: "Min Chen, Shengjun Zhang, Yuxin Li, Zhang Zhang, Xin Fei, Chong Xia, Yueqi Duan"
date: 2026-08-24
pdf: "https://arxiv.org/pdf/2608.22971v1"
tags: ["query:ad"]
score: 9.0
evidence: 具身推理、测试时扩展、长时程规划
tldr: 具身推理需在复杂环境中主动探索，但现有方法缺乏长期规划，单步前瞻难以应对延迟反馈。本文提出ParallelWorld，一种多视野测试时扩展框架，通过验证器引导的树搜索并行模拟多步轨迹，动态剪枝并优先高信息增益路径，最终优化动作序列。在ESI-Bench上显著提升主动感知与推理性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.22971v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1677, \"height\": 570, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22971v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1689, \"height\": 704, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.22971v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1719, \"height\": 1324, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.22971v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1720, \"height\": 423, \"label\": \"Table\"}]"
motivation: 现有主动推理方法多采用贪婪式增量探索或单步前瞻，无法处理复杂遮挡环境中的延迟反馈，缺乏长期规划。
method: 提出ParallelWorld，基于验证器引导的树搜索，从当前状态并行展开多条轨迹并多步仿真，每步由验证器评估剪枝，优先高信息增益路径，最终综合结果选择最优动作序列。
result: 在ESI-Bench上验证，ParallelWorld持续提升主动感知与推理性能，证明多步前瞻优于单步贪婪策略。
conclusion: 多视野并行仿真与验证器引导剪枝有效解决具身推理中的延迟反馈问题，为测试时扩展提供新范式。
---

## 摘要
具身推理是具身智能的基本能力，是在物理环境中自主感知、推理和交互的基础。近期研究已将具身推理的范式从静态感知转向动态探索，其中智能体通过与环境的交互获取任务相关信息。然而，现有的主动推理方法通常在没有长期规划的情况下逐步生成探索轨迹。即使最近出现的测试时缩放框架也常常采用短视的单步前瞻，难以解决复杂、遮挡空间环境中固有的延迟反馈问题。为解决这一局限，我们提出了平行世界（ParallelWorld），一种用于具身推理的多视野测试时缩放框架。平行世界不是贪婪的单步尝试，而是使智能体能够在执行动作之前并行模拟和评估多步未来轨迹。具体地，我们引入了一种验证器引导的树搜索范式。从当前状态出发，平行世界分支为多条并行轨迹，并在多步视野内持续展开。在每一步模拟中，验证器智能体评估中间状态转换，动态剪枝无前途的分支，并优先保留信息增益最高的路径。多步前瞻模拟完成后，智能体综合长期结果以执行最优动作序列。最后，答案智能体对所选轨迹进行推理以产生最终推理结果。在ESI-Bench上的大量实验表明，平行世界持续提升了主动感知与推理性能。

## Abstract
Embodied Reasoning constitutes a fundamental capability of embodied intelligence, serving as the basis for autonomous perception, reasoning, and interaction within physical environments. Recent studies have shifted the paradigm of embodied reasoning from static perception toward dynamic exploration, where agents acquire task-relevant information through interactions with the environment. However, existing active reasoning approaches generally generate exploration trajectories incrementally without long-horizon planning. Even recently emerged test-time scaling frameworks often resort to myopic, single-step lookaheads, which struggle to resolve the delayed feedback inherent in complex, occluded spatial environments. To address this limitation, we propose ParallelWorld, a multi-horizon test-time scaling framework for embodied reasoning. Instead of greedy, single-step trials, ParallelWorld empowers agents to simulate and evaluate multi-step future trajectories in parallel before committing to an action. Specifically, we introduce a verifier-guided tree-search paradigm. Starting from the current state, ParallelWorld branches into multiple parallel trajectories and rolls them out continuously across a multi-step horizon. At each simulation step, a verifier agent evaluates the intermediate state transitions, dynamically pruning unpromising branches and prioritizing paths with the highest information gain. Once the multi-step prospective simulation is complete, the agent synthesizes the long-horizon outcomes to commit to the optimal action sequence. Finally, an answer agent performs reasoning over the selected trajectory to produce the final reasoning. Extensive experiments on ESI-Bench demonstrate that ParallelWorld consistently improves active perception and reasoning performance.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究背景**：具身智能体需要在物理环境中通过主动感知和交互来完成任务，而传统的具身推理多基于静态观测，缺乏主动获取信息的能力。近年来的研究转向“动态探索”（active exploration），让智能体通过与环境的交互逐步收集证据。
- **核心问题**：现有的主动推理方法通常采用**逐步骤、贪婪式**的探索策略，缺乏对未来的长时程规划；即使是一些测试时缩放（test-time scaling）框架，也往往只做单步前瞻，无法应对复杂、遮挡环境中的**延迟反馈**（delayed feedback）问题。
- **解决思路**：本文提出 **ParallelWorld** 框架，通过“模拟多个可能的未来世界”来辅助决策，即在执行动作之前，并行展开和评估多条多步轨迹，从而实现更有效的主动探索和推理。

## 2. 论文提出的方法论

- **核心思想**：将测试时计算投入于“未来世界模拟”，构造一棵由可恢复模拟器状态支持的搜索树，在每个探索步骤并行展开多个分支，并用验证器（verifier）动态剪枝，最终由答案智能体基于最高评分路径给出结论。
- **总体框架**（如图1所示）：
  - 输入：问题 \(q\)、初始状态 \(s_0\)、初始观测 \(o_0\)。
  - 定义可执行动作空间：相机动作 \(A_{cam}\) 和任务相关物理动作 \(A_{task}\)（如拾取、放置、倒水、堆叠等）。
  - 维护一组保留的轨迹 \(B_t\) 及其对应的模拟世界状态 \(\xi_t=(s_t,z_t,p_t,\omega_t)\)。
- **前瞻性世界展开**：
  - 从每个保留分支出发，对所有可执行动作进行模拟转换 \( \xi_{t}^{(i,a)} = T(\xi_{t-1}^{(i)}, a) \) 并渲染相应观测 \( o_{t}^{(i,a)} \)。
  - 构建候选前沿 \(C_t\)，其规模为 \(|B_{t-1}|\times|A|\)。
  - 模拟前恢复父状态，以保证每个动作从同一世界状态评估；对有意义的中间过程保留中间帧作为证据。
- **验证器引导的搜索**：
  - 验证器 \(V_\phi\) 根据问题、参考观测、历史轨迹、候选证据和质量评分 \(v_c\) 对候选分支排序。
  - 依据预定义的分支宽度调度 \(K_t = g(t,q)\) 保留分数最高的 \(K_t\) 个分支。调度策略采用“扩张-收敛”交替模式：奇数步保留多分支以维持多样性，偶数步收敛到单一分支；并针对不同任务类别调整宽度。
  - 被选中的分支通过“重放”（replay）操作从父状态真实执行，保证观测、物体状态和模拟器状态的一致性。
- **Top-1 路由回答**：
  - 尽管验证器内部保留多个分支，但在最终决策时只允许答案智能体使用 **一条从根到叶子的一致路径**（top-1 route），避免混合互不相容世界的观测。
  - 答案智能体 \(A_\theta\) 基于问题、参考观测和该路径上的观测序列 \(E_{t}^{top1}\)，输出预测答案 \(\hat{y}_t\)、置信度 \(c_t\) 和推理 \(r_t\)。
  - 设定确定性有效性校验（如答案格式可解析），并在置信度超过阈值 \(\gamma=0.8\) 或达到最大探索深度 \(L=15\) 时停止，输出最终答案。

## 3. 实验设计

- **评测基准**：使用 **ESI-Bench**，一个具身空间智能综合基准，包含10个任务大类、29个子类。由于环境中“倒水”动作存在不可执行问题，实验排除了“液体体积”子类，实际评估28个子类。
- **对比方法**：
  - **Passive Single-View**：仅从初始视角单张观测预测答案。
  - **Active Exploration**：顺序选择相机或物理动作来逐步获取证据的主动探索基线。
  - **ParallelWorld**：本文方法（与 Active Exploration 使用相同的答案模型骨干）。
- **实现细节**：
  - 答案智能体：GPT-5.4；验证器：GPT-5.5。
  - 最大探索深度 \(L=15\)，置信度阈值 \(\gamma=0.8\)。
  - 验证器返回候选路径标识，答案智能体输出答案、置信度和推理。

## 4. 资源与算力

- 论文中**未明确说明**训练所使用的 GPU 型号、数量或具体训练时长。
- 仅在“实现细节”中提及使用了 GPT-5.4 和 GPT-5.5 作为基础模型，但这属于模型调用，而非自有模型的训练资源。
- 因此，关于算力投入无法从文中得到量化信息。

## 5. 实验数量与充分性

- **实验数量**：
  - 主实验覆盖 28 个 ESI-Bench 子类别，分属多个大类（如感知接地、物理结构、物理动态、空间关系、认知地图、枚举感知、时序理解、动作序列等）。
  - 包含一个定性分析示例，展示 ParallelWorld 选择信息量更大的分支。
  - 消融实验：比较固定分支宽度 \(K=2,3,4\) 与预设动态调度 \(K\) 调度，在 4 个子类别（Rigid Containment、Partial Occlusion、Regional Boundary、Counting with Occlusion）上报告准确率、运行时间和探索步数。
- **充分性与客观性评价**：
  - 主实验范围较广，任务类型多样，结论具有一定的代表性。
  - 消融实验仅选取了 4 个子类，覆盖面有限，未能展示所有任务类型上的宽度调度影响。
  - 实验均在仿真环境中进行，未涉及真实机器人部署；且部分子类别中 ParallelWorld 并不优于所有基线（如 Connectivity 上 Passive Single-View 最高）。
  - 总体上，实验设计基本合理，验证了核心假设，但在消融广度和真实环境验证上存在不足。

## 6. 论文的主要结论与发现

- ParallelWorld 在多数 ESI-Bench 子类上显著优于顺序式 Active Exploration，尤其体现在：
  - **时序理解**：Unobserved Change 准确率从 70.95% 提升到 89.86%。
  - **度量比较**：Spatial Distance 从 58.55% 提升到 67.11%。
  - **感知接地**：Partial Occlusion 从 57.89% 提升到 77.89%；Rigid Containment 从 60.00% 提升到 80.00%。
  - **整体趋势**：多步前瞻模拟和验证器引导剪枝优于贪婪式的单步主动探索。
- 消融实验表明，动态分支宽度策略比固定宽度更优（平均准确率 59.56% 对 55.52%/50.72%/51.87%），且平均运行时间最低（350.6 秒/问题）。
- 定性分析显示，ParallelWorld 能够通过验证器保留信息量更大的轨迹，避免沿着模糊的单轨迹走向错误答案。

## 7. 优点

- **方法创新性**：将测试时缩放从语言/静态推理扩展到具身主动推理，提出“多世界并行模拟 + 验证器剪枝”的范式，是对现有单轨迹探索的重要改进。
- **框架清晰**：将探索过程建模为可恢复模拟器上的树搜索，区分“内部多分支保留”与“最终单路径回答”，保证了物理一致性和决策可执行性。
- **实验覆盖度较好**：在 28 个任务子类上进行了系统评测，涵盖视觉、物理、认知、时序等多种挑战，结果趋势一致。
- **消融提供了实用指导**：验证了分支宽度调度对平衡探索多样性、计算效率和准确率的重要性，为实际使用提供了有效配置参考。
- **设计具有扩展性**：作者指出可将模拟器转换替换为学习的世界模型，为真实世界应用留下接口。

## 8. 不足与局限

- **计算开销大**：每个保留分支都需要对所有可执行动作进行模拟和渲染，测试时计算量随动作空间和探索深度快速增长，实际应用中可能难以承受。
- **依赖模拟器与验证器质量**：探索质量高度依赖环境模拟的保真度和验证器的评估能力；如果验证器排序不准确，剪枝可能丢失关键信息。
- **消融范围有限**：消融实验只覆盖 4 个子类，不足以全面刻画分支宽度调度在不同任务类型上的优劣；也未报告多次运行的方差。
- **缺少真实机器人验证**：实验完全在仿真环境 ESI-Bench 中进行，未考虑真实世界的噪声、执行不确定性和模拟与现实的差距。
- **部分基线对比不完整**：未与更复杂的世界模型方法（如 MindJourney 等）进行比较，仅与 Passive 和 Active 两种简单基线对比，难以充分说明相对最先进方法的优势。
- **数据集排除项**：因环境问题排除了“Liquid Volume”子类，可能影响结论的完整性。

（完）
