---
title: "Guided Riemannian Optimization (GuRO): Bridging Model Predictive Control and Decision Transformers"
title_zh: 引导黎曼优化（GuRO）：连接模型预测控制与决策Transformer
authors: "Hossein Abdi, Satya Prakash Dash, Mingfei Sun"
date: 2026-08-24
pdf: "https://arxiv.org/pdf/2608.23204v1"
tags: ["query:ad"]
score: 9.0
evidence: 面向机器人决策的统一MPC与强化学习优化
tldr: 高维非线性系统的决策任务中，模型预测控制（MPC）受限于动力学模型误差与长时域预测退化，而模型无关强化学习面临样本效率低与优化不稳定问题。本文提出GuRO框架，将MPC与决策Transformer结合，利用MPC生成的局部最优轨迹作为引导，免除离线预训练；同时采用黎曼参数空间中的曲率感知优化器，应对非凸损失地形，实现快速稳健训练。在四足机器人控制任务上，GuRO相较TRPO、SAC及在线决策Transformer等强基线，取得更高回报与更快收敛。该工作展示了模型引导与几何优化协同提升序列决策性能的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.23204v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 902, \"height\": 277, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.23204v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 893, \"height\": 360, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.23204v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 894, \"height\": 358, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.23204v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 893, \"height\": 358, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.23204v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1775, \"height\": 240, \"label\": \"Table\"}]"
motivation: MPC在模型不准确时性能退化，而强化学习样本效率低；决策Transformer虽统一二者但训练面临高度非凸优化困难。
method: 利用MPC预测的局部最优轨迹指导决策Transformer训练，并在黎曼参数空间中使用曲率感知优化器进行高效优化。
result: 在四足控制任务上，GuRO相比TRPO、SAC和在线决策Transformer等基线获得更高回报与更快收敛。
conclusion: 验证了MPC引导与黎曼优化可有效提升序列决策框架的训练性能与鲁棒性。
---

## 摘要
高维非线性系统中的决策仍然是机器人学的核心挑战。基于模型的方法如模型预测控制（MPC）虽然具有样本效率和可解释性，但当动力学模型不准确或需要长期预测时，其性能会下降。相反，无模型强化学习（RL）直接从交互中学习策略，但遭受高样本复杂性和不稳定的优化。序列建模的最新进展启发了基于Transformer的决策框架，可以统一MPC和RL，但它们的训练通常面临高度非凸损失景观带来的重大优化挑战。在这项工作中，我们提出了一种新颖的框架，将MPC与RL集成在一个序列决策框架中，并利用曲率感知优化来有效处理非凸损失景观。MPC提供局部最优轨迹的预测，指导决策Transformer，无需大量离线预训练。为了解决传统优化器缓慢且不稳定的收敛问题，我们在黎曼参数空间中使用高效的黎曼（曲率感知）方法训练策略，从而实现更快、更稳健的优化。我们在高维四足控制任务上评估了我们的框架，并展示了对强基线（包括TRPO、SAC和在线决策Transformer）的一致改进，实现了更高的回报和更快的收敛。

## Abstract
Decision-making in high-dimensional, nonlinear systems remains a central challenge in robotics. While model-based methods like Model Predictive Control (MPC) offer sample efficiency and interpretability, their performance degrades when the dynamics model is inaccurate or long-horizon predictions are required. Conversely, model-free reinforcement learning (RL) learns policies directly from interaction but suffers from high sample complexity and unstable optimization. Recent advances in sequence modeling have inspired transformer-based decision-making frameworks that can unify MPC and RL, but their training typically faces significant optimization challenges due to highly non-convex loss landscapes. In this work, we propose a novel framework that integrates MPC with RL in a sequence decision-making framework and leverages a curvature-aware optimization to efficiently tackle non-convex loss landscapes. MPC provides predictions of locally optimal trajectories that guide the decision transformer, removing the need for extensive offline pretraining. To address the slow and unstable convergence of traditional optimizers, we train the policy in a Riemannian parameter space using an efficient Riemannian (curvature-aware) method, leading to faster and more robust optimization. We evaluate our framework on high-dimensional quadruped control tasks and demonstrate consistent improvements over strong baselines, including TRPO, SAC, and Online Decision Transformer, achieving higher returns and faster convergence.

---

## 论文详细总结（自动生成）

# 论文详细总结：GuRO（引导黎曼优化：连接模型预测控制与决策Transformer）

## 1. 论文的核心问题与整体含义（研究动机与背景）

- **核心问题**：高维、非线性机器人系统（如四足机器人）的决策问题。
- **两类主流方法的各自局限**：
  - **模型预测控制（MPC）**：具备样本效率高、可解释性强等优点，但在动力学模型不准确、仅局部可靠或需长时域预测时性能显著退化。
  - **无模型强化学习（RL）**：可从交互中直接学习复杂策略、无需模型，但样本复杂度高、优化过程不稳定。
- **现有序列决策框架的不足**：决策Transformer（DT）等序列建模方法虽能统一MPC与RL的思路，但其训练面临高度非凸的损失景观，传统优化器（SGD、Adam）收敛慢、对超参数敏感，且往往依赖大规模离线预训练。
- **论文的整体目标**：构建一个统一框架，同时吸收MPC的模型推理优势与RL的无模型学习优势，并通过曲率感知的黎曼优化方法克服Transformer策略训练的优化困难，从而实现高效、稳定的策略学习。

## 2. 论文提出的方法论

### 2.1 核心思想

> 利用MPC实时生成局部最优轨迹，作为行为策略引导决策Transformer（DT）训练，免除对大规模离线预训练数据的依赖；同时将策略参数空间视为黎曼流形，使用曲率感知的Riemannian优化器替代传统欧氏优化器，以加速收敛、提高训练稳定性。

### 2.2 关键技术细节

- **序列建模框架**：采用DT范式，将轨迹表示为状态、动作、回报（return-to-go）序列，DT以自回归方式输出策略分布。
- **MPC引导机制**：
  - 每次rollout时运行有限时域MPC（horizon = T），作为行为策略输出局部最优动作序列，生成"引导轨迹"。
  - 引导轨迹与真实交互轨迹一并存入经验回放缓冲区，用于后续DT训练。
- **训练目标与约束**（最大化熵 + KL信任域约束的primal-dual问题）：
  - 主目标：最小化负对数似然（NLL）损失；
  - 熵约束：设置熵下界 δ_H，通过可学习的对偶变量 α（在log空间更新）维持策略随机性、促进探索；
  - KL信任域约束：限制每次迭代策略更新幅度，保证单调改进，使用backtracking line search强制执行。
- **高效Riemannian优化**：
  - 以Gauss-Newton估计器近似对角化Riemannian度量矩阵（基于采样动作的梯度外积）：
    - 梯度估计：ˆg（来自NLL的mini-batch估计）；
    - 瞬时度量：Ĝ = mT·diag(ĝ·ĝᵀ + εI)；
    - 指数滑动平均平滑：Ḡ_k = λḠ_{k−1} + (1−λ)Ĝ；
  - Riemannian梯度：grad L(θ) = Ḡ† ⊙ ∇θ L(θ)（元素级Hadamard积作用）；
  - 参数更新：θ ← θ − η·Ḡ† ⊙ ∇θ L(θ)，配合KL信任域回溯线搜索调节步长。
- **算法流程**：以GuRO算法伪代码（Algorithm 1）实现，核心循环包括：rollout&MPC生成轨迹 → 存入缓冲区 → 采样mini-batch → 更新对偶变量α → 估计梯度与Riemannian度量 → 计算Riemannian梯度 → 回溯线搜索更新参数。

## 3. 实验设计

### 3.1 场景与基准（Benchmark）

- **机器人平台**：Unitree AlienGo 四足机器人。
- **仿真环境**：MuJoCo XLA（MJX）物理引擎，JAX框架，GPU并行化。
- **三个挑战性任务场景**：
  1. 不平地形上的行走（locomotion on uneven terrains）；
  2. 爬楼梯（stair climbing）；
  3. 低摩擦斜坡上行（ascending a low-friction inclined surface）。

### 3.2 对比方法

- **强基线与SOTA RL算法**：TRPO（曲率感知策略优化）、SAC（最大熵RL）、Online-DT（在线决策Transformer）。
- **消融对比**（评估Riemannian优化器贡献）：同一MPC引导DT框架下分别使用SGD、Adam、Riemannian优化器（即Ours-SGD、Ours-Adam、Ours-Riemannian）。
- **评价指标**：平均总回报（mean total reward）、样本效率、收敛速度（训练损失下降速率）。

### 3.3 实现细节

- DT架构：3层Transformer解码器，128维隐藏状态，512维前馈层。
- MPC使用全身动力学模型，假设各维度不确定性标准差为0.01。
- 超参：batch size 64、MPC horizon 25、折扣因子0.99、熵下界0.2、KL信任域0.01、收缩因子0.8、ε=1e-4；网格搜索学习率和遗忘因子。
- 基线算法经有限网格搜索调参保证公平比较。

## 4. 资源与算力

- **文中明确说明**：使用 **NVIDIA GeForce RTX 4090** 单张GPU，MuJoCo XLA（MJX）+ JAX框架，并行化GPU实现（图中展示了多个并行的半透明机器人仿真）。
- **未明确说明的内容**：文中未提及GPU的具体数量、训练总时长、环境交互步数上限、能耗等量化信息。

## 5. 实验数量与充分性

- **实验组数**：
  - 3个任务场景 ×（5种对比方法 + 2种消融）= 约21组核心对比实验；
  - 每组实验覆盖5个不同随机种子，报告均值±标准差；
  - 包含消融实验（SGD vs Adam vs Riemannian）以验证优化器的独立贡献；
  - 补充了统计显著性检验（Welch's t检验和Cohen's d效应量）。
- **实验充分性评价**：
  - **优点**：覆盖了多种任务难度（地形不平、楼梯、斜坡）；与强基线系统对比；通过消融和统计检验强化结论可信度。
  - **不足**：仅在单一机器人平台（AlienGo）和单一仿真环境上评估，缺乏真实物理机器人实验或多机器人平台验证；缺少与更多近期SOTA（如PPO、Dropout Q-function等）的对比。

## 6. 论文的主要结论与发现

- **GuRO框架整体优势**：在所有三个任务中，MPC引导的DT + Riemannian优化（Ours-Riemannian）均取得最高平均总回报，优于TRPO、SAC、Online-DT以及采用SGD/Adam的自身变体。
- **样本效率提升**：MPC提供的局部最优轨迹有效减少了对真实环境交互的需求，显著提高样本效率。
- **收敛速度提升**：Riemannian优化器相比SGD和Adam在训练损失下降速度上明显更快（三个任务右侧损失曲线均佐证）。
- **统计显著性**：在不平地形任务中，与TRPO、SAC、Online-DT的差异均高度显著（p<0.01，Cohen's d分别为12.85、4.50、2.75）；在爬楼梯和低摩擦斜坡任务中，p值未全部达到0.05阈值，但效应量均为large（d>0.8），提示实际性能提升幅度可观。

## 7. 优点

- **方法创新性强**：将MPC与决策Transformer的整合从"离线数据依赖"中解放出来，利用MPC在线生成引导轨迹，兼具模型推理与无模型学习的优势。
- **优化策略先进**：在Transformer策略训练中引入曲率感知的Riemannian优化，采用Gauss-Newton对角近似+指数滑动平均，兼顾理论有效性与计算可扩展性。
- **约束设计完备**：熵约束+KL信任域+回溯线搜索，兼顾探索性与训练稳定性，解决了纯NLL训练导致的确定性收敛和更新不稳定问题。
- **实验验证严谨**：多基线对比、消融实验、多随机种子统计、统计显著性检验（t检验+Cohen's d），证据链较为完整。

## 8. 不足与局限

- **实验覆盖范围有限**：仅在单一机器人平台（Unitree AlienGo）和单一仿真器（MJX）上验证，任务类型局限在四足运动控制，缺乏对不同机器人形态（如机械臂、人形机器人）或其它决策任务的泛化验证。
- **MPC模型依赖性**：MPC生成引导轨迹的质量高度依赖标称动力学模型的准确性；文中仅在噪声标准差为0.01的条件下测试，未充分探究模型失配程度较大时框架的鲁棒性。
- **统计显著性不全**：在爬楼梯和低摩擦斜坡任务中，部分对比未达到p<0.05的显著水平，削弱了结论的统计强度。
- **消融设计尚有空间**：只消融了优化器类型（SGD/Adam/Riemannian），未独立消融MPC引导本身（如移除MPC引导、仅用随机rollout数据）对性能的贡献。
- **计算资源透明度不足**：未报告训练时长、GPU数量、能耗等算力信息，影响可复现性和实用性评估。
- **基线公平性存疑**：RL基线（TRPO、SAC）的调参程度"简单网格搜索"与GuRO的"系统网格搜索"不完全对等，可能略微利好本文方法。

---

（完）
