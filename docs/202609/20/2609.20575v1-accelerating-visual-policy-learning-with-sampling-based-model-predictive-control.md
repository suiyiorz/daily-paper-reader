---
title: Accelerating Visual Policy Learning with Sampling-Based Model Predictive Control
title_zh: 基于采样的模型预测控制加速视觉策略学习
authors: "Yilang Liu, Haoxiang You, Qian Wang, Daniel Rakita, Ian Abraham"
date: 2026-09-17
pdf: "https://arxiv.org/pdf/2609.20575v1"
tags: ["query:ad"]
score: 8.0
evidence: 面向运动与操作的视觉策略学习，结合采样模型预测控制
tldr: 视觉策略学习在足式与操作任务中需处理接触并消耗大量算力。SGPS 将采样模型预测控制的动作-目标精修与一阶策略梯度交替结合，用行为克隆初始化，并在扰动初始状态和随机动力学下短视界更新。单卡训练 Unitree Go2/G1 的移动、越障、推箱和双臂搬运，精修显著提升学习，蒸馏策略零样本迁移到真实 Go2，依靠机载深度自主小跑、爬行、跨栏并切换行为。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.20575v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 922, \"height\": 844, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.20575v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1856, \"height\": 510, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.20575v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 885, \"height\": 886, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.20575v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 885, \"height\": 318, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.20575v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 857, \"height\": 408, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.20575v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 922, \"height\": 427, \"label\": \"Figure\"}]"
motivation: 视觉策略学习需处理接触，算力与显存开销大；FoPG 依赖可微仿真，但局部优化易收敛到非预期接触模式。
method: 提出 SGPS，用采样 MPC 反复精修动作-目标，并与短视界 FoPG 交替；行为克隆初始化，解耦渲染图，直接从 depth 学习。
result: 单 GPU 上在 Unitree Go2/G1 学会移动、越障、推箱、双臂搬运；精修优于仅初始化或跟踪，蒸馏策略零样本部署真机。
conclusion: SGPS 将采样 MPC 与一阶策略优化结合，提升视觉策略训练效率与鲁棒性，实现真实 Go2 机载深度自主多行为切换。
---

## 摘要
为移动和操作学习视觉策略需要与环境接触进行协调，并可能带来大量计算和GPU内存开销。一阶策略梯度（FoPG）通过可微分仿真降低训练成本，但局部优化可能收敛到非预期的接触模式。为弥补这一不足，我们提出采样引导策略搜索（SGPS），它将基于采样的模型预测控制中反复进行的动作-目标细化与一阶策略优化相结合。行为克隆从采样动作初始化策略；随后训练在扰动初始状态和随机动力学下，交替进行基于采样的细化与短时域FoPG更新。对于视觉策略训练，我们采用解耦的FoPG形式，将渲染排除在计算图之外，从而无需状态-策略教师即可直接从深度观测中学习。在单块GPU上，SGPS为仿真Unitree Go2和G1机器人学习移动、越障、推箱子和双臂搬运策略。实验进一步表明，细化优于仅初始化和跟踪，能改进策略学习。对于硬件部署，蒸馏后的策略零样本迁移到真实Go2，并利用机载深度自主小跑、匍匐、跨越栏架，并在这些行为之间切换。

## Abstract
Learning visual policies for locomotion and manipulation requires coordinating contact with the environment and can incur substantial computation and GPU memory costs. First-order policy gradients (FoPG) reduce training cost through differentiable simulation, but local optimization can converge to unintended contact patterns. To address this shortfall, we propose Sampling-Guided Policy Search (SGPS), which couples recurring action-target refinement by sampling-based model-predictive control with first-order policy optimization. Behavior cloning initializes the policy from sampled actions; training then alternates sampling-based refinement with short-horizon FoPG updates under perturbed initial states and randomized dynamics. For visual policy training, we use a decoupled FoPG formulation that excludes rendering from the computation graph, enabling direct learning from depth observations without a state-policy teacher. On a single GPU, SGPS learns policies for locomotion, obstacle traversal, crate pushing, and bimanual carrying on simulated Unitree Go2 and G1 robots. Our experiments further show that refinement improves policy learning beyond initialization and tracking alone. For hardware deployment, the distilled policy transfers zero-shot to a real Go2 and uses onboard depth to autonomously trot, crawl, clear hurdles, and switch between these behaviors.

---

## 论文详细总结（自动生成）

# 论文总结：Accelerating Visual Policy Learning with Sampling-Based Model Predictive Control

## 1. 核心问题与整体含义（研究动机与背景）

- **视觉策略学习成本高**：为足式机器人与操作任务学习视觉策略需要协调与环境接触，训练需大量仿真步数与额外计算/显存开销（渲染、视觉观测）。
- **现有降本路径存在缺陷**：
  - 常用"先训特权状态教师、再蒸馏视觉学生"的两阶段流程（如 [2][11]-[13]），增加训练复杂度与成本。
  - 一阶策略梯度（FoPG，如 SHAC、D.Va）通过可微仿真降低训练成本，D.Va 进一步用解耦 FoPG 将渲染排除出计算图，支持从深度观测直接学习。
  - **核心痛点**：FoPG 的局部优化在奖励无法区分步态时，可能收敛到非预期的接触模式（如"盲步态"导致的拖步）。
- **采样 MPC 的潜力与局限**：
  - 采样型 MPC（如 DIAL-MPC）通过无导数优化生成动态可行的运动轨迹，可作为参考行为引导策略更新。
  - 但部署 MPC 需要在线优化与状态估计（DIAL-MPC 硬件实验依赖动捕），难以直接作为策略执行。
- **论文目标**：提出 **SGPS（Sampling-Guided Policy Search）**，将采样 MPC 的反复动作-目标精修与解耦 FoPG 结合，把 MPC 轨迹"翻译"为可容忍偏差、无需在线 MPC 的视觉策略，并实现真机零样本部署。

## 2. 方法论：核心思想、关键技术细节与算法流程

### 2.1 核心思想
- 以采样 MPC 提供 **固定跟踪参考** 与 **行为克隆初始化**；训练中交替进行 **采样精修（生成新动作目标）** 与 **短时域解耦 FoPG 更新**（在扰动初始状态与随机动力学下），从而兼顾搜索的全局探索与梯度的局部精调。
- 跟踪参考保持固定，精修只更新动作目标，不改变行为目标。

### 2.2 关键技术细节
- **问题建模**：可微动力学 $s_{t+1}=f(s_t,a_t)$，观测 $o_t=g(s_t)$（$g$ 可不可微）；策略为 tanh-squashed 高斯（式 1），目标为最大化折扣回报（式 2）。
- **解耦 FoPG**：沿用 SHAC 的 $h$ 步窗口 + 终端 critic 损失（式 3）；演员在 $sg[o_t]$（停止梯度）处评估，将观测构造（含渲染）排除出计算图，但保留动作与动力学到策略（含视觉编码器）的梯度。
- **参考生成（退火采样，仿 DIAL-MPC）**：
  - 用 $M+1$ 个样条节点解码动作序列；候选节点按 $\sigma_{i,m}=\beta_{iter}^i \beta_{hor}^{M-m}$ 加噪并裁剪（式 4）。
  - 以训练模型上的任务奖励 $R_{task}$（可不可微）加权更新名义计划（式 5），执行首动作后重规划。
  - 参考轨迹需通过预设的任务级成功测试后才被采纳。
- **行为克隆初始化**：对每个参考状态加 $K_{aug}$ 个噪声副本，观测 $g(\hat{s}_{t,k})$ 配同一参考动作；用 $\text{atanh}(\text{clip}(a^*))$ 转为预 squash 目标（式 6），最小化 BC 损失（式 7）。
- **训练流程（Algorithm 1）**：$J=25$ 个 block，每 block 先精修、后 $B=80$ 个 actor-critic epoch。
  - **参考跟踪奖励**（式 8）：$R_{track}=w_{alive}+\sum_k w_k \exp[-c_k d_k]$，涵盖机身/基座位姿、速度、足高、关节角等。
  - **采样精修**（式 9-11）：从当前训练状态出发，状态策略用确定性 rollout 播种计划，视觉策略用零动作名义计划；用带缩放噪声的退火采样搜索，仅当段平均跟踪得分满足 $\bar{R}_{train}(\tilde{\tau})\ge \max(\kappa, \bar{R}_{train}(\tau_{seed})+\delta)$ 才接受。
  - **联合损失**（式 12）：$L^{(j)}_{SGPS}=L_{track}+\lambda_j L_{BC}$，$\lambda_j=\lambda_0(1-j/J)$ 线性衰减，目标 buffer 每 block 固定、下一 block 替换。
- **视觉策略学习**：专家输入为 3 帧 64×64 深度图（小型 CNN 编码）+ 53 维（Go2）/106 维（G1 推）/118 维（G1 搬）跟踪观测；深度从 detached 状态渲染，物理仍可微；训练时随机将部分初始化批次的深度栈置零，防止策略利用图像恒等性走捷径。
- **命题 1（搜索目标蒸馏）**：当采样目标等于"返回梯度步"$\tilde{Z}=Z_0+\beta\nabla_Z J(Z_0)$ 时，目标拟合梯度方向等价于确定性解耦 FoPG 方向（式 15），从理论上连接采样监督与 FoPG。

## 3. 实验设计

- **仿真平台与任务**：
  - 仿真器：MuJoCo MJX；机器人：Unitree Go2（12 维动作）、Unitree G1（29 维动作，50 Hz 关节位置指令）。
  - 任务：Go2 平地小跑（0.8 m/s）、Go2 视觉越障（匍匐过横梁、跨越 0.10 m 栏架）、G1 推 18 kg 箱子（0.08 m/s）、G1 双臂搬运 4 kg 箱子（0.5 m/s 慢跑）。
- **Benchmark**：以 Go2 平地 0.8 m/s 前向小跑为主要基准（步态相位、占空比 0.45、步频 2 Hz、摆动高 0.08 m、基座高 0.30 m）。
- **对比方法**：
  - **BC only**（仅行为克隆初始化后停止）
  - **FoPG from scratch**（随机参数起步）
  - **PPO**（无模型 RL 基线）
  - **State-to-Vision DAgger**（先训状态专家再蒸馏视觉学生）
  - **SGPS + refinement** vs **SGPS, no refinement**（消融）
- **行为选择消融**：将关节角跟踪权重 $w_{joint}$ 置零，训练"盲步态"策略，用对角足接触相关性评估（参考 +0.86，SGPS +0.78，盲步态 +0.21）。
- **硬件部署**：Unitree Go2 EDU，机载 NVIDIA Jetson Orin（8 GB 共享内存），Intel RealSense D435i 深度相机、IMU、关节编码器、足力传感器；无动捕、无外部定位、无激光雷达里程计、无在线 MPC。控制环 50 Hz，深度编码器 10 Hz。
- **技能组合**：用 DAgger 将 trot/crawl/hurdle 三个专家蒸馏为统一视觉策略（式 17），部署观测含深度、本体感知、历史、学习状态估计与速度指令。

## 4. 资源与算力

- **训练硬件**：单张 **NVIDIA RTX 4080 GPU**（文明确说明 "on a single NVIDIA RTX 4080 GPU"）。
- **仿真后端**：MuJoCo MJX。
- **训练预算**：标准 2000 epochs，约 **4.096M FoPG rollout 交互**（另加搜索 rollout）；FoPG 使用 $h=32$ 步窗口、64 个并行环境。
- **训练时长**：不同任务约 25 分钟 ~ 100+ 分钟（图 3、图 5 的 wall-clock 横轴，含精修搜索与热启动）；文中提到视觉搬运任务在约 1 小时内达到约 25,000 回报并成功完成任务。
- **硬件推理**：机载 GPU TorchScript 推理平均 2.7 ms（99 分位 4.3 ms），完整控制步平均 11 ms。
- **未明确说明**：未给出精确的 GPU 显存占用、总训练轮次的绝对能耗，也未报告多卡扩展性。

## 5. 实验数量与充分性

- **实验组数概览**：
  - 主任务：Go2 小跑（状态 + 视觉）、Go2 匍匐、Go2 跨栏、G1 推箱、G1 搬运。
  - 每个 G1 任务与 actor 模态用 **3 个随机种子**。
  - 消融：SGPS + refinement vs no refinement（Go2 状态/视觉、G1 视觉搬运）。
  - 行为选择消融：盲步态奖励（$w_{joint}=0$）。
  - 基线对比：BC only、FoPG scratch、PPO、DAgger 专家/学生。
  - 硬件实验：统一视觉策略零样本部署，自主行为切换。
- **充分性评价**：
  - **较充分**：覆盖状态与视觉两种模态、四类任务、多组基线与消融，且给出 3 seed 的方差阴影。
  - **客观性**：所有曲线为标称模型上的确定性评估；计算比较同时报告 wall-clock 与环境步数（含搜索 rollout），考虑了精修成本，较为公平。
  - **公平性局限**：消融未分离门控、搜索初始化与克隆调度的影响；G1 任务仅 3 seed；未与更多 MPC-蒸馏类方法（如 MPC-guided policy search 的现代变体）在同等算力下系统对比。

## 6. 主要结论与发现

- **学习效率**：SGPS 在状态与视觉观测下均能达到高回报；PPO 使用的环境交互次数约为 SGPS 的 **196 倍**；视觉学习中 SGPS 可直接训练，而 DAgger 需先单独训练状态专家。
- **精修有效**：在 Go2 视觉小跑中，约 4.1M 交互时完整 SGPS 达约 **16,000** 回报，而无精修版本停在约 **11,000**；G1 视觉搬运中约 6M 交互时完整 SGPS 约 **26,500**，无精修约 **20,500**。
- **行为选择**：无关节角监督且从零训练的盲步态策略产生弱协调拖步（对角相关 +0.21），远低于参考（+0.86）与 SGPS（+0.78），说明 BC 初始化与关节角监督对恢复目标步态必要。
- **视觉越障**：SGPS 无需状态教师即可直接学习匍匐与跨栏视觉策略。
- **人形操作**：G1 推箱与搬运均可学会；视觉搬运在约 1 小时内达到约 25,000 回报并成功执行，约 4.1M 交互后超过 DAgger 教师。
- **硬件部署**：蒸馏的统一 Go2 视觉策略零样本迁移到真机，仅凭机载深度与速度指令自主完成小跑、匍匐、跨栏及行为切换，无需参考输入或在线 MPC。

## 7. 优点

- **方法创新性强**：首次将采样型 MPC 的反复动作-目标精修与解耦 FoPG 结合，兼顾无导数搜索的全局性与可微梯度的效率。
- **无需特权教师**：视觉策略直接从深度观测训练，避免两阶段"状态教师→视觉学生"流程。
- **理论支撑**：命题 1 给出搜索目标蒸馏与解耦 FoPG 方向的等价关系，为方法提供解释。
- **单卡可行**：单张 RTX 4080 即可完成多任务训练，训练成本可控。
- **消融设计清晰**：固定跟踪参考、仅切换精修，隔离了精修与初始化的贡献。
- **真机零样本迁移**：完整机载感知-推理-控制链路，无动捕/外部定位，自主行为切换，工程落地性强。
- **公平的计算对比**：wall-clock 与环境步数双口径，且将搜索 rollout 计入成本。

## 8. 不足与局限

- **依赖可微动力学**：SGPS 需动力学可微（物理部分），对强接触、非光滑动力学场景适用性受限。
- **依赖成功的采样参考**：参考需通过任务级成功测试，若采样 MPC 无法生成可行参考，方法失效。
- **依赖任务特定目标**：采样目标 $R_{task}$ 需人工设计（步态相位、占空比、高度航点等），迁移到新任务需重新调参。
- **人形任务简化**：推箱与搬运简化了物体运动与手-物交互（如软腕约束、单轴滑动、自由箱关节），复杂操作（抓取、多接触、长时域操作）未探索。
- **硬件覆盖有限**：仅真机验证 Go2，G1 仅仿真；未做人形硬件部署。
- **消融粒度**：未单独分离接受门控、搜索初始化、克隆调度的影响；未与更多 MPC-蒸馏基线在同等算力下对比。
- **理论边界**：命题 1 仅在生成参数处成立，后续有限步朝固定目标拟合不等于 FoPG 更新；采样目标不保证提升期望回报。
- **未明确资源细节**：未报告显存占用、能耗、多卡扩展性与总训练成本上限。

（完）
