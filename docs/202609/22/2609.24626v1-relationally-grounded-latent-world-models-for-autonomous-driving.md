---
title: Relationally Grounded Latent World Models for Autonomous Driving
title_zh: 面向自动驾驶的关系锚定潜在世界模型
authors: "Fabian Schmidt, Markus Enzweiler, Abhinav Valada"
date: 2026-09-21
pdf: "https://arxiv.org/pdf/2609.24626v1"
tags: ["query:ad"]
score: 9.0
evidence: 面向自动驾驶、以场景图语义监督的潜在世界模型
tldr: "潜在世界模型能预测自动驾驶场景，但其状态往往缺乏显式关系语义。本文基于 LAW，从 nuScenes 三维标注构建以行为体为中心的场景图，用冻结文本嵌入编码关系结构，并在训练中对齐视觉潜在表示。推理时移除监督分支，不依赖场景图或三维标注且不增加测试计算。在 nuScenes 上，轨迹平均 L2 误差由 0.661 降至 0.622（5.9%），碰撞率由 0.456 降至 0.217（52.4%），且优于无结构描述监督，表明显式关系结构有助于潜在世界模型表征学习。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.24626v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 825, \"height\": 263, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.24626v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 905, \"height\": 338, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.24626v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1814, \"height\": 914, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.24626v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 211, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.24626v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 824, \"height\": 506, \"label\": \"Table\"}]"
motivation: 潜在世界模型的关系语义常是隐式的，本文探索交通场景图能否作为特权语义监督，以提升自动驾驶预测表征。
method: 基于 LAW，从 nuScenes 3D 标注构建以行为体为中心的场景图，用冻结文本嵌入编码关系结构，并训练时对齐视觉潜在表示；推理移除监督分支。
result: "在 nuScenes 上，轨迹平均 L2 误差从 0.661 降至 0.622（5.9%），碰撞率从 0.456 降至 0.217（52.4%），并优于无结构描述式语义目标。"
conclusion: 显式关系结构可作为有效特权监督，提升潜在世界模型表征，且不增加测试时计算与标注依赖。
---

## 摘要
潜在世界模型为自动驾驶学习预测性表征，但这些状态所保留的关系语义往往仍是隐式的。我们研究交通场景图能否作为潜在世界表征的特权语义监督。基于 LAW，我们从 nuScenes 3D 标注构建以参与者为中心的场景图，使用冻结的文本嵌入模型编码其序列化关系结构，并在训练期间将视觉潜在表征与该语义目标对齐。我们在推理时移除监督分支，因此它既不需要场景图也不需要 3D 标注，并且不增加测试时计算。在 nuScenes 上，相对于我们重新训练的 LAW 基线，我们的方法将平均轨迹 L2 误差从 0.661 降至 0.622（5.9%），并将碰撞率从 0.456 降至 0.217（52.4%）。它还优于非结构化的描述式语义目标，支持显式关系结构对潜在世界模型表征学习的益处。

## Abstract
Latent world models learn predictive representations for autonomous driving, but the relational semantics these states preserve often remain implicit. We investigate whether traffic scene graphs can serve as privileged semantic supervision for latent world representations. Building on LAW, we construct actor-centric scene graphs from nuScenes 3D annotations, encode their serialized relational structure using a frozen text embedding model, and align the visual latent representations with this semantic target during training. We remove the supervision branch at inference, so it requires neither scene graphs nor 3D annotations and adds no test-time computation. On nuScenes, our method reduces average trajectory L2 error from 0.661 to 0.622 (5.9%) and collision rate from 0.456 to 0.217 (52.4%) relative to our retrained LAW baseline. It also outperforms an unstructured caption-style semantic target, supporting the benefit of explicit relational structure for latent world-model representation learning.

---

## 论文详细总结（自动生成）

# 论文总结：Relationally Grounded Latent World Models for Autonomous Driving

**论文信息**：Fabian Schmidt, Markus Enzweiler, Abhinav Valada，arXiv:2609.24626v1。

## 1. 核心问题与整体含义

- **研究动机**：潜在世界模型能够为自动驾驶学习预测性表征，但这些潜在状态中保留的“关系语义”通常是隐式的。模型虽然能预测未来潜在状态、接受几何先验和规划目标监督，却没有显式约束交通参与者之间的关系。
- **核心问题**：交通场景图能否作为一种“特权语义监督”，在训练阶段约束潜在世界模型的视觉潜在表征，从而提升下游规划性能，同时不增加推理阶段的计算和标注依赖？
- **整体含义**：论文试图把结构化关系信息注入潜在世界模型。其目标不是改变推理架构，而是利用训练时可得的三维标注和场景图，让视觉潜在表征学习到对交互驾驶更重要的相对位置、朝向、邻近和可见性等关系语义。
- **背景脉络**：端到端自动驾驶从统一感知—规划架构发展到基础模型式智能体，语言感知方法和世界模型方法都强调语义结构化表征。LAW 是代表性潜在世界模型，GraphWorld、GraphPilot 等工作说明结构化场景图有价值，但“关系结构是否可作为仅在训练时使用的特权监督”仍未充分探索。

## 2. 方法论

- **核心思想**：基于 LAW，在不改变原模型推理路径的前提下，增加一个训练时的辅助对齐分支：将 LAW 的视觉潜在表征与交通场景图的语义嵌入对齐，使潜在状态隐含地保留关系语义；训练结束后丢弃监督分支。
- **场景图构建与序列化**：
  - 对每个训练帧，从 nuScenes 三维标注构建场景图 \(G_t=(N_t,E_t)\)。
  - 采用 GraphPilot 的 Actor-Only 表示：节点为自车及周围车辆、骑行者、行人等交通参与者；边编码相对朝向、邻近、可见性等关系。
  - 将图序列化为“主体—关系—客体”式结构化文本 \(s_t=\mathrm{Serialize}(G_t)\)。
- **语义目标编码**：
  - 使用冻结的 Qwen3-Embedding 文本模型编码序列化场景图，得到语义目标 \(z^{SG}_t=E_{text}(s_t)\)。
  - 文本嵌入模型不参与梯度更新。
- **视觉潜在对齐**：
  - LAW 的视觉编码器从多视图图像 \(I_t\) 提取视觉潜在集合 \(V_t\)。
  - 对齐头 \(P\) 对视觉潜在做均值池化，再经 MLP 投影到同一语义空间：\(z^{vis}_t=P(V_t)=\mathrm{MLP}(\mathrm{MeanPool}(V_t))\)。
  - 使用余弦距离对齐视觉嵌入与场景图嵌入：\(L_{SG}=d_{cos}(z^{vis}_t,z^{SG}_t)\)。
  - 梯度通过视觉编码器和对齐头传播。
- **LAW 原始流程**：
  - 视觉编码器提取视觉潜在，路点解码器预测未来自车轨迹 \(W_t\)。
  - 将轨迹向量化并与每个视觉潜在拼接，经 MLP 得到动作感知特征 \(a
