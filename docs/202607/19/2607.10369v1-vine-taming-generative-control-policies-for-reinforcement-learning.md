---
title: "VINE: Taming Generative Control Policies for Reinforcement Learning"
title_zh: VINE：驯服强化学习的生成控制策略
authors: "Rushuai Yang, Zhuo Han, Houlin Li, Hecheng Wang, Zhichao Wu, Rui Zhang, Zhaowei Zhang, Zihong Chen, Xiaohan Yan, Chiming Liu, Yi Chen, Wei Shan, Maoqing Yao"
date: 2026-07-11
pdf: "https://arxiv.org/pdf/2607.10369v1"
tags: ["query:ad"]
score: 9.0
evidence: 在机器人学习中扩展生成策略的价值梯度强化学习
tldr: 流匹配策略在机器人学习中有高表达能力，但结合值梯度强化学习时训练不稳定。现有方法认为不稳定性源于迭代生成，因此牺牲了迭代过程或端到端优化。本文发现不稳定性实际来自行为克隆中使用的采样策略，为此提出VINE方法，通过在每个去噪步重建新插值状态来创建稳定的可微路径。VINE在不牺牲迭代生成或值梯度优化的前提下，在OGBench离线RL基准和真实机器人操作任务中持续超越最先进方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10369v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1414, \"height\": 463, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 778, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1436, \"height\": 538, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1300, \"height\": 644, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1284, \"height\": 742, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1389, \"height\": 1347, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1389, \"height\": 1347, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10369v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1465, \"height\": 427, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10369v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1433, \"height\": 208, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10369v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1500, \"height\": 653, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10369v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1213, \"height\": 389, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10369v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1544, \"height\": 1685, \"label\": \"Table\"}]"
motivation: 流匹配策略在值梯度强化学习中存在训练不稳定问题，现有方法为此牺牲了迭代生成或端到端优化。
method: 提出VINE采样方法，在每个去噪步重建新插值状态，建立稳定可微路径，兼容原始去噪过程。
result: 在OGBench离线RL基准和真实机器人操作任务上，VINE持续优于最先进的强化学习方法。
conclusion: 揭示了不稳定性源于采样策略而非迭代生成，并提出VINE实现稳定端到端值梯度优化，保持流匹配的表达力。
---

## 摘要
流匹配策略已成为机器人学习中的一种有效策略参数化方法。它们从噪声中迭代生成动作，从而能够对复杂和多模态的动作分布进行高度表达性的建模。然而，先前的研究观察到，将这些策略与值梯度强化学习（RL）结合时，常常导致训练不稳定。现有方法将这种不稳定性归因于迭代生成，因此通过牺牲迭代生成、高表达性或值梯度优化来避免端到端的值梯度优化。与先前观点相反，我们表明不稳定性并非源于迭代生成本身，而是源于最初为行为克隆设计的朴素采样策略，该策略在值梯度强化学习下变得脆弱。基于这一见解，我们提出了VINE，一种面向强化学习的采样方法，能够对流匹配策略实现稳定的端到端值梯度优化。VINE不是沿着单一流轨迹，而是在每个去噪步骤重构一个新的插值状态，为值梯度传播创建一条稳定的可微路径，同时保持与原始流匹配去噪过程的兼容性。因此，VINE保留了流匹配的表达性和迭代生成，而不牺牲端到端的值梯度优化。尽管通过所有十个去噪步骤进行端到端反向传播，VINE仍能实现稳定的策略改进，并在OGBench离线强化学习基准和真实世界机器人操作任务上持续优于最先进的RL方法。视频可在我们的网站上获取：https://agibottech.github.io/vine。

## Abstract
Flow-matching policies have emerged as an effective policy parameterization for robot learning. They iteratively generate actions from noise, enabling highly expressive modeling of complex and multimodal action distributions. However, prior works observed that scaling these policies with value-gradient reinforcement learning (RL) often leads to training instability. Existing methods attribute this instability to iterative generation and therefore avoid end-to-end value-gradient optimization by sacrificing iterative generation, high expressiveness, or value-gradient optimization. Contrary to prior belief, we show the instability does not stem from iterative generation itself, but from the vanilla sampling strategy originally designed for behavior cloning, which becomes brittle under value-gradient RL. Motivated by this insight, we propose VINE, an RL-oriented sampling method that enables stable end-to-end value-gradient optimization for flow-matching policies. Instead of following a single flow trajectory, VINE reconstructs a new interpolation state at every denoising step, creating a stable differentiable path for value-gradient propagation while remaining compatible with the original flow-matching denoising process. As a result, VINE preserves the expressiveness and iterative generation of flow-matching without sacrificing end-to-end value-gradient optimization. Despite performing end-to-end backpropagation through all ten denoising steps, VINE achieves stable policy improvement and consistently outperforms state-of-the-art RL methods on the OGBench offline RL benchmark and real-world robotic manipulation task. Videos are available on our website: https://agibottech.github.io/vine.

---

## 论文详细总结（自动生成）

# 论文中文结构化总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：流匹配（Flow Matching）等生成控制策略在机器人学习的行为克隆中取得了成功，因为它们能通过迭代去噪表达复杂、多模态的动作分布。然而，当使用值梯度强化学习（RL）对这些策略进行微调时，却普遍遭遇训练不稳定性，导致性能下降。
- **现有认知的误区**：先前工作将不稳定归因于迭代生成过程本身，因此采取三种妥协策略：①冻结生成策略参数，用外部引导信号控制去噪；②将多步生成蒸馏为单步策略；③放弃使用评论家（Critic）的动作梯度，仅将值估计作为标量权重。这些方法都牺牲了表达能力、迭代生成或值梯度优化的部分优势。
- **本文核心发现**：作者指出，不稳定性并非来自迭代生成本身，而是来自最初为行为克隆设计的**采样策略**（即Euler ODE采样器）。在RL微调中，该策略无法支持评论家梯度引导的价值驱动模式切换，从而导致梯度传播路径不稳定。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：重新设计面向RL的采样方法，称为**VINE（Value-gradient Iterative Noise Exploration）**。VINE在每个去噪步骤中，基于当前动作估计重构一个新的插值噪声状态，再利用原始流匹配速度场进行一步预测，从而构建一条稳定的可微路径，使得评论家梯度可以通过所有去噪步骤进行端到端反向传播。
- **关键技术细节**：
  - 标准流匹配训练：使用插值公式 \(x_t = t a + (1 - t)z\)（a为真实动作，z为高斯噪声）。
  - VINE采样过程（Algorithm 1）：
    1. 初始化动作估计 \(\hat{a}_0 \sim \mathcal{N}(0, I)\)。
    2. 对于每个去噪步 \(k = 0,...,K-1\)：
        - 设置时间 \(t_k = k/K\)。
        - 基于当前动作估计 \(\hat{a}_k\)，采样一个新的噪声 \(z_k\)，重构插值状态：\(\hat{x}_k = t_k \hat{a}_k + (1 - t_k)z_k\)。
        - 使用流匹配速度场预测下一步动作估计：\(\hat{a}_{k+1} = \hat{x}_k + (1 - t_k)v_\theta(\hat{x}_k, t_k; s)\)。
    3. 最终动作 \(a = \hat{a}_K\)。
  - 理论保证：定理1证明，最优流匹配速度场 \(v^*\) 满足 \(\hat{a}_{k+1} = \mathbb{E}[a | \hat{x}_k, t_k, s]\)，即VINE的预测是贝叶斯最优动作估计，与标准流匹配训练兼容。推论1进一步证明在确定性设置下VINE会单调收缩到均值，保证收敛。
- **与现有方法的区别**：VINE不牺牲迭代生成（保持K=10步），不冻结生成策略，也不放弃动作梯度，而是通过重构插值状态使梯度传播路径稳定。

## 3. 实验设计：数据集、基准、对比方法

- **离线RL实验**：
  - **基准**：**OGBench**，包含8个域共40个任务：antmaze-large, antmaze-giant, humanoidmaze-medium, humanoidmaze-large, scene-sparse, puzzle-3x3-sparse, cube-double, cube-triple。每个域有5个任务。
  - **对比方法**：按策略更新方式分为四组：
    - **高斯策略**：ReBRAC (行为正则化)。
    - **Around-the-Chain**：FQL, FAWAC, IFQL, QAM, DAC, QSM, CGQL变体等（避免通过全链BPTT）。
    - **Outside-the-Chain**：DSRL, FEdit, FBRAC, BAM（冻结或外部优化）。
    - **Through-the-Chain**：VINE（直接通过全链BPTT）。
  - 评估协议：每任务12个种子，报告均值及95%置信区间（bootstrap 5000次）。
- **真实世界在线RL实验**：
  - **任务**：机器人**插头插入插座**（接触式精密操作），需要从桌面拿起插头并插入固定插座。
  - **对比方法**：SAC-Flow, Hil-SERL, DSRL, RLT, EXPO。其中DSRL、RLT、EXPO使用预训练策略初始化，VINE、SAC-Flow、Hil-SERL为零样本初始化。
  - 评估指标：20次试验的成功率、在线微调时间、人工干预比例。

## 4. 资源与算力

- 论文**未明确说明**使用的GPU型号、数量或训练时长。仅提到：
  - 离线VINE训练1M梯度步（每个任务），评估每20K步。
  - 真实世界实验在线微调时间约20分钟。
  - 未提及硬件资源细节（如A100、V100等）。

## 5. 实验数量与充分性

- **离线实验**：覆盖8个域共40个任务，每个任务12个种子，共约480次独立训练。对比了13种以上基线方法，覆盖不同类别。结果以表格（表1、表5）和训练曲线（附录图6、7）呈现，统计置信区间计算方式合理。
- **真实世界实验**：1个任务，20次试验，对比6种基线。但受现实条件限制，规模较小。
- **消融实验**（Section 5.4）：
  - 采样策略消融：VINE vs 标准Euler采样器，在5个OGBench任务上比较成功率和梯度范数稳定性。
  - 迭代步数消融：K=1,4,6,8,10，验证迭代精细化的必要性。
- **充分性评价**：离线实验覆盖域广泛，统计严谨；消融实验直接验证核心假设。但真实世界实验仅一个场景，泛化性验证有限。总体而言实验设计合理、客观，但文中未讨论超参数敏感性（如α的选取仅列在表4中）。

## 6. 论文的主要结论与发现

1. **不稳定性根源**：流匹配策略在值梯度RL中的不稳定性主要源于标准Euler采样器的单噪声轨迹，而非迭代生成本身。
2. **VINE有效性**：通过每步重构插值状态，VINE能稳定传播评论家梯度，并在保持10步去噪的情况下，实现稳定的策略改进。
3. **性能优越**：在OGBench 40个任务上，VINE取得最高平均得分（60% vs 第二名FQL的55%等），尤其在人形迷宫等长程任务上提升显著。
4. **实际部署可行**：在真实机器人插头插入任务中，VINE取得20/20成功率，微调时间最短（20分钟），人工干预最低（19.2%）。

## 7. 优点

- **创新性**：首次明确指出采样策略而非迭代生成本身是不稳定关键，并提供理论证明（定理1）支撑新采样与原始流匹配训练的一致性。
- **实现简洁**：仅需修改采样器，无需改变训练目标或辅助网络，可直接应用于预训练流匹配策略。
- **实验全面**：离线40任务+真实世界任务，统计严谨；消融实验验证核心设计（采样策略、迭代步数）。
- **实际价值**：在真实机器人任务上取得SOTA，且微调时间短、人工干预少，表明方法可落地。

## 8. 不足与局限

- **真实实验规模有限**：仅一个真实任务（插头插入），未在多种操作任务上验证泛化性。
- **超参数敏感性未深入分析**：行为克隆系数α在各任务差异很大（10~1000），但未讨论其选择策略或稳健性。
- **计算成本**：虽然未报告算力，但VINE需要进行K步网络前向和反向传播，相比单步策略计算开销更大。文中未与同类方法（如FQL）比较计算效率。
- **理论局限**：定理1依赖最优速度场，实际训练中速度场为近似，可能的偏差未分析。
- **离线RL限制**：VINE基于行为正则化的actor-critic框架，在极端稀疏奖励或数据覆盖不足时可能仍存在问题（如cube-triple任务VINE得分仅1%）。
- **与更大型模型的结合**：文中提到未来方向包括扩展到大型基础模型，但当前实验仅使用中等规模MLP/ResNet，未见与Vision-Language-Action模型（如π0）的融合验证。

（完）
