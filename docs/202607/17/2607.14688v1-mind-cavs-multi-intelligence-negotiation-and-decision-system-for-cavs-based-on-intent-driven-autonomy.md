---
title: "MIND-CAVs: Multi-Intelligence Negotiation and Decision System for CAVs based on Intent-Driven Autonomy"
title_zh: MIND-CAVs：基于意图驱动自主的网联自动驾驶多智能体协商与决策系统
authors: "Mainak Mondal, Yihang Feng, Yangchao Luo, Han Song"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14688v1"
tags: ["query:ad"]
score: 9.0
evidence: 联网自动驾驶车辆的多智能体协商决策
tldr: 当前自动驾驶车辆孤立运行，仅广播低级状态，缺乏高层意图通信，导致全局协调决策不一致。提出MIND-CAVs框架，每车将感知抽象为结构化意图表示并通过V2X交换，边缘服务器结合学习与规则仲裁冲突意图，云平台记录决策。在CARLA多车道高速场景中，相比孤立自主、先到先服务和多智能体强化学习基线，机动完成时间缩短，不安全接近和必要制动减少。该工作为CAVs提供了意图驱动的全局协调决策机制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14688v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1745, \"height\": 770, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14688v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1828, \"height\": 670, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14688v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 909, \"height\": 362, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14688v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 904, \"height\": 366, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14688v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 906, \"height\": 369, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14688v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1506, \"height\": 585, \"label\": \"Table\"}]"
motivation: 当前CAVs缺乏高层意图通信和全局协调机制，导致决策不一致和安全隐患。
method: 每车将感知抽象为意图表示，通过V2X交换，边缘服务器结合学习与规则仲裁意图冲突。
result: 在CARLA多车道高速场景中，相比基线，机动时间缩短，不安全接近和必要制动减少。
conclusion: 基于意图驱动的多智能体协商框架可有效实现全局一致的协同决策。
---

## 摘要
现代自动驾驶车辆大多作为独立智能体运行：它们依赖车载感知与决策模块，并广播仅包含低级运动状态的基本安全消息（BSM）。尽管现有的协同驾驶框架实现了有限的传感器共享，但它们很少传递高级机动意图，且边缘计算主要用于内容分发而非决策仲裁。因此，当前的网联自主缺乏一种原则性机制来跨车辆做出全局一致、意图感知的协调决策。为填补这一空白，我们提出MIND-CAVs——一种基于意图驱动自主的网联自动驾驶多智能体协商与决策框架。每辆车将原始传感器观测抽象为结构化意图表示，通过V2X链路交换，并从路侧边缘服务器接收全局一致的协调计划。边缘智能体结合学习与基于规则的仲裁机制来协商车辆间的冲突意图，而云平台记录决策以用于审计和持续重训练。我们在基于CARLA的AI在环平台上实现MIND-CAVs，并在涉及冲突机动和路线约束出口的多车道高速公路场景中进行评估。实验结果表明，与独立自主、先到先得仲裁以及多智能体强化学习基线相比，该方法改善了机动完成时间，减少了不安全接近和不必要制动。

## Abstract
Modern autonomous vehicles largely operate as isolated agents: they rely on on-board perception and decision modules and broadcast Basic Safety Messages (BSMs) that expose only low-level kinematic state. While existing cooperative driving frameworks enable limited sensor sharing, they rarely communicate high-level maneuver intentions, and edge computing is primarily used for content delivery rather than decision arbitration. As a result, current connected autonomy lacks a principled mechanism for making globally consistent, intent-aware coordination decisions across vehicles. To address this gap, we propose MIND-CAVs, a Multi-Intelligence Negotiation and Decision framework for connected autonomous vehicles (CAVs) based on intent-driven autonomy. Each vehicle abstracts raw sensor observations into structured intent representations, exchanges them over V2X links, and receives globally consistent coordination plans from roadside edge servers. Edge agents combine learned and rule-based arbitration mechanisms to negotiate conflicting intents among vehicles, while a cloud platform records decisions for auditing and continual retraining. We implement MIND-CAVs in a CARLA-based AI-in-the-loop platform and evaluate it in multi-lane highway scenarios involving conflicting maneuvers and route-constrained exits. Experimental results show improved maneuver completion time and reduced unsafe proximity and unnecessary braking compared with isolated autonomy, first-come-first-served arbitration, and multi-agent reinforcement learning baselines.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：当前自动驾驶车辆大多作为孤立智能体运行，仅广播低级运动状态（如BSM/CAM消息），缺乏高层机动意图的通信，导致全局协调不一致、决策保守、交通安全和效率低下。现有的协同驾驶框架（如传感器共享、集中调度）要么难以与V2X栈集成，要么不包含结构化协商与仲裁机制，且缺乏可审计的决策记录。
- **整体含义**：本文提出MIND-CAVs，一个基于意图驱动自主的多智能体协商与决策框架。通过引入分层车辆-边缘-云架构，允许车辆显式交换结构化意图，由边缘服务器进行安全仲裁，同时云平台记录决策日志以支持审计和再训练。目标是实现全局一致、可解释、安全高效的网联自动驾驶协调。

### 2. 论文提出的方法论

- **核心思想**：采用三层智能架构：
  - **Vehicle Intelligence (VI)**：每辆车将传感器观测抽象为结构化**意图-计划对**（intent-plan pair），其中意图定义为 `I_t^i = ⟨g, a, τ, C⟩`（目标、机动类、执行时间、安全约束），计划 `P_t^i` 为一系列高阶机动原语序列。车辆通过V2X将意图-计划对发送给最近的MEC节点，并等待仲裁响应；若超时则回退到保守策略。
  - **Edge Intelligence (EI)**：MEC节点维护活动数据库 `D_t^j`，收到请求后使用**约束视觉语言模型（VLM）** 结合规则进行语义仲裁，输出ACK（批准）、PLAN（修订）或NACK（拒绝）。仲裁过程包括：① 构建包含上下文、方案、约束的提示；② VLM单次推理；③ 确定性时空验证器强制安全约束（最小距离、加减速边界）；④ 更新数据库并生成审计记录。优先级规则：紧急车辆优先，否则按提交时间FCFS。
  - **Cloud Intelligence (CI)**：异步接收结构化审计记录 `R_t^i = ⟨I, P, ˆP, o, d⟩`，支持离线回放、性能分析和数据集生成，不参与实时控制。
- **关键技术细节**：
  - 效用函数：`U(v_t^i) = α·速度 − β·不必要制动指示 − γ·安全危险距离指示`，用于引导仲裁目标。
  - 算法1：VLM辅助仲裁（VLM-assisted arbitration w/ safety guard），包含提示构造、推理、安全验证、提交日志。
  - 通信流：基线V2X广播 + 意图叠加信道 + 审计传播信道。
  - 延迟分析：ACK约335ms，PLAN修订约690ms，在高速下仍可接受。

### 3. 实验设计

- **场景设置**：在CARLA模拟器中（Town 04，直线四车道高速，无背景交通），使用自定义AI-in-the-loop平台（MIND-CAVs Simulation Conductor）。
- **三个场景**：
  - **S1**：单智能体多车道变换（左1车辆变至最右车道，左2车辆保持车道）。
  - **S2**：冲突车道变换（左1变至最右，左2同时变至最左）。
  - **S3**：路线约束出口机动（左1车辆需在60秒内到达右侧出口，左2保持）。
- **对比方法**：
  - **Isolated Autonomy (IA)**：无意图交换或仲裁，仅本地感知。
  - **First-Come-First-Serve (FCFS)**：集中式确定性仲裁，按请求时间排序，无计划修订或意图推理。
  - **Multi-Agent Reinforcement Learning (MARL)**：多智能体强化学习策略，共享状态，学习协作但无显式意图协商。
- **评估指标**：机动完成时间、安全间距违规（<5m）、不必要制动（速度下降>5km/h且无其他车在50m内）。

### 4. 资源与算力

- 仿真平台硬件：Intel i7-10700 CPU (2.90 GHz)，64 GB RAM，NVIDIA RTX 3070 GPU（运行Ubuntu 24.04 + CARLA 0.9.15）。
- 车辆端VLM推理：Jetson AGX类硬件，4-bit量化，约270ms生成意图与计划（~20 tokens）。
- MEC服务器推理：A100类硬件，吞吐量约22 tokens/s，ACK约45ms，PLAN约400ms。
- **未明确说明训练时长、GPU数量或训练数据量**，仅给出推理延迟估计。

### 5. 实验数量与充分性

- **数量**：每个场景运行10次（初始配置相同，VLM采样独立）。共3场景 × 4方法 = 120次运行。
- **充分性**：
  - 优点：统计结果以均值±标准差和95%置信区间报告，有显著性（MIND-CAVs在多个指标上明显优于基线）。
  - 不足：场景较简单（仅2辆车、直线高速、无背景交通、无感知噪声），未包含城市交叉口、混合交通、恶劣天气等复杂情况；未做消融实验（例如去掉安全验证器的效果、不同VLM模型的对比）；实验次数较少可能不足以覆盖VLM的随机性。

### 6. 论文的主要结论与发现

- MIND-CAVs在所有场景中均获得**最低的机动完成时间**（S1缩短24~29%，S2缩短约10~15%，S3缩短约3~6%）。
- **安全优势**：在S1中零间距违规和不必要制动；在S2和S3中违规次数与基线相当或更低，且不必要制动明显减少。
- **稳定协调**：完成时间方差低于基线，行为更可预测。
- 结论：意图驱动仲裁结合安全门控可同时提升效率与安全性，而不引入不稳定。

### 7. 优点（方法与实验设计亮点）

1. **分层架构**：车辆-边缘-云三层合理分工，边缘负责实时仲裁，云负责离线审计和再训练，兼顾实时性与可扩展性。
2. **意图显式化**：将高层机动意图结构化为机器可读格式，与V2X兼容，比纯状态广播更有信息量。
3. **可审计性**：每次决策生成结构化审计记录（intent, plan, rationale等），支持确定性回放和监管审查，这是许多协同系统所缺乏的。
4. **安全门控**：VLM推理后经确定性空间-时间验证器修正，防止错误决策被执行，安全有保障。
5. **对比基线全面**：覆盖独立自主、启发式仲裁和学习型方法，实验设计公平、客观。
6. **开放平台**：CARLA模拟和日志记录详细，有助于复现与后续扩展。

### 8. 不足与局限

- **场景有限**：仅限结构化高速公路、两辆车、无背景交通、无感知噪声，与现实复杂城市交通差距大。
- **实验统计量少**：每个场景仅10次，VLM采样随机性可能未被充分捕捉；未报告多次运行的不同种子分布，仅均值方差。
- **速度限制**：实验中MEC强制将车速上限设为50 km/h（因API速率限制），不反映实际高速，可能影响效率指标的绝对数值。
- **VLM可靠性**：仲裁依赖VLM语义推理，非形式化验证；VLM可能产生幻觉或对提示敏感，虽然有安全门控，但正式保证缺失。
- **未提及实际部署**：仅在仿真中运行，未在实物小车或真实路测中验证延迟和鲁棒性。
- **消融实验缺失**：未分离分析意图交换、计划修订、安全验证等组件各自的贡献。
- **网络理想化**：假设V2X通信低延迟、无丢包，未评估通信故障或恶意攻击的影响。

（完）
