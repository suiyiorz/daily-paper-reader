---
title: "S-squared-VLA: Decoupling Semantic and Spatial Streams in Vision-Language-Action Models for Autonomous Driving"
title_zh: S-squared-VLA：面向自动驾驶的视觉-语言-行动模型中语义与空间流的解耦
authors: "Jianguo Yu, Rukang Wang, Duanfeng Chu, Chen Wang, Renju Feng, Liping Lu"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13926v1"
tags: ["query:ad"]
score: 9.0
evidence: 在自动驾驶VLA中解耦语义与空间流
tldr: "现有视觉-语言-动作模型（VLA）在自动驾驶中因语义与空间信息纠缠导致空间表征崩溃，难以生成精确控制动作。为此，本文提出S-squared-VLA，显式解耦语义和空间流：语义流通过分层桥接提取多尺度VLM特征进行意图推理，空间流绕过自回归瓶颈直接保留未压缩视觉空间特征并引入辅助感知监督。双流规划适配器利用级联注意力融合高层语义与精细空间约束。在NAVSIM闭环基准上，该方法在纯监督微调下达到87.1的PDMS和98.4%的无碰撞率，刷新VLA模型最佳性能，有效缓解了传统VLM的空间表征塌缩问题。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13926v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 912, \"height\": 1020, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13926v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1877, \"height\": 869, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13926v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 920, \"height\": 740, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13926v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 567, \"height\": 506, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13926v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1537, \"height\": 542, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13926v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1626, \"height\": 226, \"label\": \"Table\"}]"
motivation: VLA模型因语义与空间信息纠缠导致空间表征崩溃，无法保留细粒度几何先验，阻碍精确轨迹规划。
method: 解耦语义与空间双流：语义流分层提取多尺度VLM特征，空间流直接保留未压缩视觉特征并加感知监督，双流适配器级联融合。
result: "在NAVSIM基准上PDMS达87.1，无碰撞率98.4%，均超越现有VLA模型，且优于其他方法。"
conclusion: S-squared-VLA通过解耦双流设计缓解了空间表征塌缩，实现了高精度闭环控制，为VLA在自动驾驶中的实用化提供了新范式。
---

## 摘要
视觉-语言模型（VLM）在自动驾驶的高层推理中展现出显著潜力，但它们本质上难以生成精确的低层控制动作。这一局限源于离散语言标记与连续轨迹规划之间固有的不匹配所导致的语义-物理鸿沟。尽管视觉-语言-行动（VLA）架构试图通过将感知与控制统一为单一策略来弥合这一鸿沟，但这种纠缠却带来了新的瓶颈。标准VLA模型会经历严重的空间表征崩溃，这不可逆地劣化了安全、边界感知导航所必需的细粒度空间和几何先验。为解决这一局限，我们提出了S-squared-VLA，它明确地将视觉-语言-行动模型中的语义流与空间流解耦。语义流利用层次化桥接提取多尺度VLM特征以进行鲁棒的意图推理。同时，独立的空间流绕过了自回归语言瓶颈，直接保留来自视觉编码器的未压缩空间特征。通过整合辅助感知监督，该流显式地赋予模型丰富的空间和几何先验。最后，一个双流规划适配器通过级联注意力机制将高层语义意图与精确定位约束融合。在NAVSIM闭环基准上的评估表明，S-squared-VLA在纯监督微调（SFT）设置下达到了87.1的预测驾驶员模型分数（PDMS），为VLA模型创造了新的最先进水平。通过缓解传统VLM的空间表征崩溃，我们的框架显著优于基线，在所有评估方法中实现了最高的无碰撞率（NC）98.4。

## Abstract
Vision-Language Models (VLMs) have demonstrated remarkable potential for high-level reasoning in autonomous driving, yet they fundamentally struggle to generate precise, low-level control actions. This limitation is rooted in a semantic-physical gap caused by the inherent mismatch between discrete language tokens and continuous trajectory planning. While Vision-Language-Action (VLA) architectures attempt to bridge this gap by unifying perception and control into a single policy, this entanglement creates a new bottleneck. Standard VLAs experience a severe spatial representation collapse, which irreversibly degrades the fine-grained spatial and geometric priors essential for safe, boundary-aware navigation. To address this limitation, we propose the S-squared-VLA, which explicitly decouples the semantic and spatial streams in Vision-Language-Action models. The semantic stream leverages hierarchical bridging to extract multi-scale VLM features for robust intent reasoning. In parallel, an independent spatial stream bypasses the autoregressive language bottleneck, directly preserving uncompressed spatial features from the visual encoder. By integrating auxiliary perception supervision, this stream explicitly equips the model with rich spatial and geometric priors. Finally, a dual-stream planning adapter fuses high-level semantic intent with precise spatial constraints via cascaded attention mechanisms. Evaluations on the NAVSIM closed-loop benchmark show that S-squared-VLA achieves a Predictive Driver Model Score (PDMS) of 87.1, establishing a new state-of-the-art for VLA models under a purely supervised fine-tuning (SFT) setting. By mitigating the spatial representation collapse of traditional VLMs, our framework significantly outperforms baselines, achieving the highest No Collision (NC) rate of 98.4 among all evaluated methods.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：视觉-语言模型（VLM）在自动驾驶中擅长高层语义推理（如场景理解、决策解释），但难以生成精确连续的底层控制信号，根源在于**离散语言 token 与连续轨迹规划之间的语义-物理鸿沟**。尽管视觉-语言-行动（VLA）模型试图通过统一感知与控制的单策略框架来弥合这一鸿沟，但现有 VLA 将语义推理与空间感知强耦合，导致**严重的空间表征崩溃**——细粒度的空间几何先验在自回归语言处理过程中被不可逆地损失，进而使轨迹规划不安全、边界意识弱。
- **研究意义**：提出一种解耦语义与空间流的新范式，克服 VLA 固有的语义-几何偏差，在保证高层推理能力的同时，保留精确几何约束，实现安全高效的闭环自动驾驶。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：显式解耦语义流与空间流，分别负责高层意图推理和低层空间约束，再用双流规划适配器进行融合。

- **关键技术细节**：
  - **多尺度语义流**：
    - 使用 InternVL3-2B 作为视觉-语言骨干。
    - 注入 64 个可学习**动作查询（Action Queries）**，引导网络提取与执行相关的特征。
    - 采用**稀疏多尺度采样**：仅从骨干网络的选定层（第 3、8、13、18、23、24 层）提取隐藏状态，获取互补的细粒度与高层语义特征。
    - 结合 Ego 历史状态（通过 MLP 编码）形成统一状态记忆。
  - **任务驱动空间流**：
    - 从视觉编码器（ViT）直接提取未压缩的视觉特征，绕过语言自回归瓶颈。
    - 引入 64 个可学习**视觉查询**，通过自注意力聚合细粒度几何信息。
    - 动态分辨率策略（1个全局缩略图+8个局部裁剪），并添加 tile 级位置编码。
    - **辅助感知监督**：
      - **地图头**：将 1D 特征重塑为 2D 网格，通过上采样和卷积输出局部 BEV 语义地图（32m×64m），使用加权交叉熵损失。
      - **代理头**：基于 DETR 范式，用 30 个可学习查询和 Transformer 解码器预测动态障碍物的边界框与类别，使用 L1 回归+BCE 分类损失。
    - 通过多任务监督，空间流被严格正则化以编码度量布局和边界。
  - **双流规划适配器**（级联多阶段交叉注意力解码器）：
    - 初始化 8 个可学习规划令牌。
    - **第一阶段（语义与状态对齐）**：规划令牌与多尺度语义特征、Ego 状态记忆进行交叉注意力，并使用可学习门控融合。
    - **第二阶段（视觉空间细化）**：融合后的令牌与空间流的密集几何特征交叉注意力，注入显式空间约束。
    - **轨迹解码**：最后通过 MLP 预测 8 个未来路径点，损失包括 L1 回归和运动平滑性约束（加速度、加加速度）。
  - **整体损失**：$L_{total} = \lambda_{plan} L_{plan} + \lambda_{agent} L_{agent} + \lambda_{map} L_{map}$，平衡规划、代理感知和地图重建。

## 3. 实验设计
- **数据集/基准**：NAVSIM 闭环基准，侧重于代理与动态环境的交互。评价指标为 **PDMS（Predictive Driver Model Score）**，由 5 个子指标组成：无碰撞率（NC）、可行驶区域合规率（DAC）、通行时间（TTC）、舒适性（Comf）、自我进度（EP）。
- **对比方法**：
  - **E2E 方法**：TransFuser、UniAD、PARA-Drive、DRAMA、ARTEMIS、DiffusionDrive（部分使用 LiDAR）。
  - **VLM/VLA 方法**：InternVL3-2B（基础 VLM）、ImagiDrive、AutoVLA、ReCogDrive。
- **设置**：所有模型均在**纯监督微调（SFT）** 下评估，排除后训练的 RL（如 GRPO），以保证网络设计公平比较。

## 4. 资源与算力
- 论文明确说明：所有实验使用 **4 块 NVIDIA A100 GPU**，优化器为 AdamW。
- 训练策略分为三阶段：
  1. 在 ReCogDrive VQA 数据集上对预训练 VLM 进行 SFT 3 个 epoch。
  2. 冻结 VLM，使用 LoRA 训练意图提取和辅助感知模块 4 个 epoch。
  3. 冻结感知与意图模块，仅优化视觉细化模块 4 个 epoch。
- 未明确给出总训练时长，但从 epoch 数和 batch size 16 可推断训练成本可控。

## 5. 实验数量与充分性
- **主要定量实验**：一个主表（Table II）对比了 11 种方法，包含所有子指标。
- **消融实验**：一个消融表（Table III）逐步添加语义特征、空间流、辅助感知，共 4 行，清晰展示各组件贡献。
- **定性分析**：与 Transfuser、InternVL3-2B、ReCogDrive 的三组轨迹对比可视化（图 4）。
- **充分性评估**：
  - 实验设计较为严谨：排除了后训练干扰，对比了多种代表性方法，覆盖了 E2E、VLM、VLA 三大类。
  - 消融实验验证了每个模块的增量收益，逻辑链条清晰。
  - 但因 NAVSIM 仅为闭环仿真基准，未在真实世界数据集（如 nuScenes open-loop）上验证，存在一定局限性。
  - 实验组数（1主表+1消融表+3组可视化）数量适中，足以支撑核心结论。

## 6. 主要结论与发现
- **性能突破**：S²-VLA 在纯 SFT 设置下达到 **PDMS 87.1**，超越所有 VLM/VLA 基线（如 ReCogDrive 86.5、ImagiDrive 86.4），甚至超过多数 LiDAR 融合的 E2E 方法（如 ARTEMIS 87.0、DRAMA 86.9）。
- **安全提升**：**无碰撞率 NC 达 98.4%**，为所有评估方法最高，表明空间流有效避免了障碍物碰撞。
- **原因分析**：解耦双流设计成功保留了细粒度空间几何信息，避免了语言 token 量化导致的精度损失；辅助感知任务为规划器提供了明确的物理边界。
- **局限性未被解决**：DiffusionDrive（88.1 PDMS）仍优于本方法，但该方法依赖 LiDAR 和迭代扩散解码器，计算更重。

## 7. 优点
- **架构创新**：显式解耦语义与空间流，从根本上解决了传统 VLA 的空间表征崩溃问题，设计思路简洁且有效。
- **工程精巧**：稀疏多尺度特征采样、动态分辨率、可学习查询注入、级联双流适配器，平衡了效率与精度。
- **辅助感知监督**：地图头与代理头为空间流注入显式几何先验，无需额外 3D 传感器即可实现强空间理解。
- **实验公平性**：严格限定纯 SFT 设置，对比基线排除了 RL 后训练，结论可靠。

## 8. 不足与局限
- **计算开销**：双流密集特征提取与融合带来额外的计算成本，论文本身也承认这是主要局限，并计划未来采用稀疏特征机制。
- **未采用闭环 RL**：虽然排除了 RL 后训练以保证公平，但作者指出双流架构与 RL 正交，若结合可能进一步提升性能，当前实验未探索这一方向。
- **传感器依赖**：仅使用单目前视相机，虽在单目条件下取得很好成绩，但与 LiDAR 深度融合的方法（如 DiffusionDrive）仍有差距，且未验证在恶劣天气或光照下是否稳健。
- **基准局限性**：仅在 NAVSIM 闭环仿真上评估，缺乏真实世界开放道路或 nuScenes open-loop 验证，泛化能力尚未完全证明。
- **消融覆盖略少**：未讨论语义流中稀疏采样层数选择、查询数量、损失权重等超参数敏感性，分析深度可进一步加强。

（完）
