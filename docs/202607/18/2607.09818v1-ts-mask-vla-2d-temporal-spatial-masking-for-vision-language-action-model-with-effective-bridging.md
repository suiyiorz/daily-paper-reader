---
title: "TS-Mask VLA: 2D Temporal-Spatial Masking for Vision-Language-Action Model with Effective Bridging"
title_zh: TS-Mask VLA：面向视觉-语言-动作模型的时间-空间二维掩码与高效桥接
authors: "Shengzhuo Yang, Ronghao Yu, Chuanjie Lv, Linpeng Peng, Hang Yu, Jie Ren, Jiajun Lv, Yong Liu"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09818v1"
tags: ["query:ad"]
score: 8.0
evidence: 通过时空掩码的视觉-语言-动作模型进行机器人操作
tldr: "现有VLA模型将动作生成简化为下一个词预测，缺乏对动作时空结构的显式建模及视觉-语言表示与动作的解耦，限制了长程和复杂场景下的性能。为解决此问题，本文提出TS-Mask VLA，其核心包括离散扩散动作专家（通过桥接注意力实现多层条件桥接）和时空2D掩码策略（增强跨时间依赖和维度耦合）。在LIBERO基准上，仅0.5B参数模型即达到95.7%平均成功率，超越多个更大模型；在CALVIN上取得4.19最佳平均序列长度；真实世界实验也验证了其有效性。该方法有效提升了动作生成的结构性和稳定性，为机器人操作领域提供了新的解决方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09818v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1824, \"height\": 815, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09818v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 891, \"height\": 423, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09818v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1839, \"height\": 628, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09818v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 903, \"height\": 1156, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09818v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1762, \"height\": 625, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.09818v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1846, \"height\": 573, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09818v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1846, \"height\": 587, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09818v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 786, \"height\": 163, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09818v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 777, \"height\": 141, \"label\": \"Table\"}]"
motivation: 现有VLA模型将动作生成视为下一个词预测，缺乏对动作时空结构的显式建模及视觉-语言与动作的解耦，限制了在长程和复杂场景下的性能。
method: 提出TS-Mask VLA，采用离散扩散动作专家配合桥接注意力条件桥接实现多层条件桥接，并设计时空2D掩码策略以增强跨时间依赖和维度耦合，从而提升动作生成的准确性和稳定性。
result: "在LIBERO基准上，仅0.5B参数模型达到95.7%平均成功率，超越多个更大模型；在CALVIN上取得4.19最佳平均序列长度；真实世界实验也验证了有效性。"
conclusion: TS-Mask VLA通过有效的条件桥接和时空掩码，显著提升了动作生成的结构性和长程任务性能，推动了VLA在机器人操作中的应用。
---

## 摘要
视觉-语言-动作模型旨在理解自然语言指令和视觉观察，并作为具身智能体生成并执行相应动作。近年来，基于自回归词元的动作生成推动了众多代表性VLA模型的发展。然而，该范式通常将动作生成简化为下一个词元预测，从而缺乏对动作序列时空结构的显式建模以及视觉-语言表示与动作之间的解耦，这可能会限制其在长时域和复杂场景中的性能。本文提出TS-Mask VLA，一种面向机器人操作的视觉-语言-动作框架。TS-Mask VLA基于两个关键设计：(1) 配备桥接注意力条件桥的离散扩散动作专家，该桥接结构实现了来自VLM的多层条件注入，有助于更准确、稳定的动作生成；(2) 针对离散动作词元的时间-空间二维掩码策略，增强了模型对跨时间依赖性和维度间耦合的理解，从而产生结构更一致的动作序列。我们在仿真基准和现实任务上进行了大量实验。在LIBERO上，TS-Mask VLA仅用0.5B参数即达到95.7%的平均成功率，显著优于更大的模型。在CALVIN上，它取得了最佳平均序列长度4.19和强大的长时域性能。全面的分析和消融实验进一步验证了我们设计的有效性。

## Abstract
Vision-language-action (VLA) models aim to understand natural-language instructions and visual observations, and to generate and execute corresponding actions as embodied agents. Recently, autoregressive token-based action generation has driven the development of many representative VLA models. However, this paradigm often reduces action generation to next-token prediction, thereby lacking explicit modeling of the spatiotemporal structure of action sequences and the disentanglement between vision-language representations and actions, which can limit performance in long-horizon and complex scenarios. In this paper, we propose TS-Mask VLA, a vision-language-action framework for robot manipulation. TS-Mask VLA is built upon two key designs: (1) a Discrete Diffusion Action Expert equipped with a Bridge Attention conditioning bridge, which enables multi-layer conditioning from the VLM and facilitates more accurate and stable action generation; and (2) a temporal-spatial 2D masking strategy for discrete action tokens that strengthens the model's understanding of cross-time dependencies and inter-dimensional coupling, leading to more structurally consistent action sequences. We conduct extensive experiments on simulation benchmarks and real-world tasks. On LIBERO, TS-Mask VLA achieves a 95.7 percent average success rate with only 0.5B parameters, outperforming significantly larger models. On CALVIN, it attains the best average sequence length of 4.19 and strong long-horizon performance. Comprehensive analyses and ablations further validate the effectiveness of our design.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- 现有视觉-语言-动作（VLA）模型大多基于自回归词元预测，将动作生成简化为下一个词预测，缺乏对动作序列时空结构的显式建模。
- 同时，视觉-语言表示与动作生成之间缺乏有效解耦，导致在长时域（long-horizon）和复杂场景下性能受限。
- 已有两种主流范式：自回归变换器（如 OpenVLA）和连续扩散方法（如 Diffusion Policy），各自存在结构耦合不足或时空建模困难的问题。
- 本文旨在通过离散化动作并引入结构化的时空掩码机制，提升动作生成的准确性与稳定性。

## 2. 方法论
### 核心思想
- 提出 TS-Mask VLA，核心包含两个设计：
  - **离散扩散动作专家（Discrete Diffusion Action Expert）**：利用桥接注意力（Bridge Attention）从 VLM 多层提取条件信息，注入动作生成过程。
  - **时间-空间二维掩码策略（Temporal-Spatial 2D Masking）**：将离散动作组织为 T×D 的 2D 网格，沿时间轴和动作维度分别进行结构化的掩码，增强跨时间依赖和维度间耦合。

### 关键技术细节
1. **多模态视觉语言编码**：
   - 使用 DINOv2 和 SigLIP 提取第三视角图像和腕部相机图像特征，通过 MLP 投影到语言嵌入空间。
   - 采用 Qwen2.5-0.5B 作为 VLM 骨干，加入可学习的 Action Query (AQ) 令牌。
   - 提取所有 L 层隐藏状态 H = {H^(l)}，用于层级条件的注入。

2. **离散扩散动作专家**：
   - 由多层 Bridge Attention 块和 FFN 组成，与 VLM 层对齐。
   - **Bridge Attention**：每个块融合三股信息：自身令牌（自注意力）、AQ 令牌（动作导向条件）、任务令牌（视觉语言特征）。使用可学习门控系数 g 调节任务分支，缓解早期过度条件化。
   - **动作离散化**：将连续动作归一化到 [-1,1]，均匀量化到 256 个区间（索引 0-255），形成 M = T×D 的离散序列，并可重塑为 T×D 的 2D 结构。

3. **时间-空间 2D 掩码**：
   - 先进行**时间掩码**：随机选择 round(r·T) 个时间帧，将该帧内所有 D 个动作维度全部掩码。
   - 再对剩余帧进行**空间掩码**：每帧内随机掩码 round(r·D) 个词元。
   - 掩码比例 r 由余弦调度器确定：r = cos(πt/2)，t ~ Uniform(0,1)。

4. **训练损失**：
   - 主损失 L_mask：仅对掩码位置计算交叉熵。
   - 附加 unroll loss L_unroll：第一次预测后用高置信度结果回填，再对剩余掩码位置预测并计算损失。
   - 最终损失 L = (L_mask + λ L_unroll) / (1 + λ)，实验中 λ=0.5 最佳。

5. **推理流程**：
   - 从全掩码序列 t0 开始，迭代 I 步。
   - 每步预测掩码位置的词元概率，采样得到候选，并计算置信度。
   - 使用 ReMask 策略：将低置信度词元重新掩码，掩码数量 mi = ⌈γ(i/I)·|Ωi|⌉，γ 为余弦调度。
   - 逐步精细化，直至得到完整离散动作序列，再通过反量化为连续动作。

## 3. 实验设计
### 数据集/场景
- **仿真基准**：
  - **LIBERO**：包含 LIBERO-Spatial、LIBERO-Object、LIBERO-Goal、LIBERO-Long 四个任务套件，覆盖空间推理、目标理解、泛化和长时域操作。
  - **CALVIN**：采用 ABC→D 协议，评估多步骤语言条件任务序列（最多连续 5 个任务）的成功率和平均序列长度。
- **真实世界实验**：使用 UR5e 机械臂和 Robotiq-85 夹爪，部署 RGB-D 摄像头（第三视角+腕部），设计三个任务：放置苹果、拉出纸巾、翻转平底锅，每种物体放置于不同位置以增加难度。

### 对比方法
- 按规模分类对比：
  - **Large**：FlowVLA (8.5B)、OpenVLA (7B)、CoT-VLA (7B)、ThinkAct (7B) 等。
  - **Small**：π0 (3B)、π0-FAST (3B)、SmolVLA (2.2B)、GR00T N1 (2B) 等。
  - **Tiny**：Seer (0.57B)、VLA-OS (0.5B)、Diffusion Policy、VLA-Adapter-Pro (0.5B)、MoDE (0.44B) 等。
- 基线包括非 VLM 方法（如 Diffusion Policy）和主流 VLA。

## 4. 资源与算力
- 文中明确：**训练在单个 NVIDIA RTX 4090 GPU 上完成**。
- 采用 LoRA 参数高效微调策略，行动块长度设为 8。
- **未提及具体训练时长、总步数或 epoch 数**。

## 5. 实验数量与充分性
- **LIBERO**：报告了所有四个套件及平均成功率，对比 10+ 个基线。
- **CALVIN**：报告了任务 1-5 的完成率及平均序列长度。
- **消融实验**：
  - 掩码策略对比（1D 随机掩码 vs 2D 时空掩码）：在 LIBERO 四个套件上分别测试。
  - 步展开损失强度（λ = 0, 0.5, 1.0）在 LIBERO-Spatial 上测试。
- **真实世界**：三个任务各 20 次试验，对比 OpenVLA-OFT 和 π0。
- **充分性评估**：实验覆盖了不同任务类型、规模、长时域场景，消融针对关键设计（掩码和损失），对比了多个代表性基线。但未报告多次运行的标准差，也未进行跨数据集泛化测试或交叉验证，因此统计分析相对薄弱，但整体设计较为全面客观。

## 6. 主要结论与发现
- **LIBERO**：TS-Mask VLA（0.5B）平均成功率达 **95.7%**，超过所有对比方法，包括 FlowVLA（8.5B，88.1%）、π0（3B，94.2%）等。尤其在 LIBERO-Long 上达 91.6%，显著优于同类模型。
- **CALVIN**：平均序列长度 **4.19**，5 任务完成率 **66.9%**，均超越 7B 级模型（如 OpenVLA-OFT 为 4.10 和 66.5%）。
- **消融**：2D 时空掩码优于 1D 掩码，尤其在长时域任务中提升 6.6%。适度步展开损失（λ=0.5）显著改善成功率（+4.6%），而过大则有害。
- **真实世界**：在三个操作任务中均优于 OpenVLA-OFT 和 π0，验证了方法的实际有效性。

## 7. 优点
- **轻量高效**：仅 0.5B 参数即达到甚至超越大模型（7B-8.5B）性能，参数效率极高。
- **创新架构**：离散扩散动作专家结合桥接注意力，实现多层条件注入，优于简单自回归或连续扩散。
- **结构先验强**：2D 时空掩码显式建模时间和动作维度的结构，提供明确归纳偏置。
- **长时域能力突出**：在 LIBERO-Long 和 CALVIN 长序列任务上表现优异，验证了设计的有效性。
- **实验全面**：覆盖模拟（两个主流基准）和真实机器人场景，消融实验验证关键组件。
- **单 GPU 可训练**：降低了可重复性和部署门槛。

## 8. 不足与局限
- **资源限制明确**：仅使用单 GPU（RTX 4090），未探索更大规模训练资源下的潜力，作者自述为未来工作。
- **缺少统计可靠性**：未报告多次运行的平均值和标准差，可能受随机性影响。
- **任务多样性有限**：虽然包含模拟和真实场景，但真实世界仅三个简单操作任务（放置、拉出、翻转），未覆盖复杂装配或动态交互。
- **缺乏与同类离散扩散 VLA 的直接对比**：论文未与 Discrete Diffusion VLA（如引文[1]）等类似方法比较，消融对照不够完整。
- **泛化性验证不足**：仅在一个机械臂平台（UR5e）上测试，未在不同构型或环境中验证。
- **依赖均匀量化**：可能损失细粒度动作信息，与 VQ-VAE 相比，作者声称避免了过度压缩，但均匀量化的表达能力有限。
- **推理步骤 I 未明确**：文中提到迭代 I 步，但未给出具体值，可能影响实际效率和延迟。

（完）
