---
title: "UESF-Bench: Benchmarking and Probing for Unified Embodied Seeking and Following"
title_zh: UESF-Bench：统一具身搜索与跟随的基准测试与探究
authors: "Kun Yu, Jianhua Yang, Yixiang Chen, Changwei Wang, Hongyuan Yu, Yan Huang, Fushuo Huo, Ya Jing, Zhumin Chen, Keji He"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13621v1"
tags: ["query:ad"]
score: 9.0
evidence: 具身智能体在物理环境中搜索与跟随人类的基准
tldr: 现有具身智能benchmark通常假设目标在起点可见，忽略了先搜索再跟踪的联合需求。本文提出UESF-Bench，一个大规模统一基准，要求智能体处理语义引导探索、行为切换与延迟身份确认。同时提出SeekFollow-VLA框架，通过任务驱动路由机制实现隐藏阶段推断与过渡建模。实验表明该方法在单人与多人场景中均超越基线，为统一寻找与跟随任务建立基准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13621v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1812, \"height\": 503, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13621v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 764, \"height\": 337, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13621v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1806, \"height\": 897, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13621v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 875, \"height\": 539, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13621v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 753, \"height\": 324, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13621v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 894, \"height\": 218, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13621v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 892, \"height\": 219, \"label\": \"Table\"}]"
motivation: 现有benchmark将寻找与跟随作为分离任务，缺乏统一评估标准，且假设目标初始可见，不满足真实动态环境需求。
method: 提出UESF-Bench统一基准，并设计SeekFollow-VLA框架，包含视觉-语言-动作模型与任务驱动路由机制，实现寻找与跟随的联合推理与行为切换。
result: 在单人与多人环境中，SeekFollow-VLA相比单头与双头基线均取得显著提升，验证了统一框架的有效性。
conclusion: UESF-Bench填补了统一寻找-跟随基准的空白，SeekFollow-VLA为复杂具身任务提供了可扩展的解决方案。
---

## 摘要
语言引导的人类跟随是具身智能体的重要能力，但现有基准测试通常假设目标人物在回合开始时可见。这种设置简化了问题，忽略了一个更现实的需求：智能体通常需要先找到语言描述的目标，然后在动态环境中持续跟随该目标。尽管最近的工作开始研究人类搜索，但现有设置通常基于特定任务场景进行评估，并常常依赖于更强的环境先验知识。此外，它们通常将搜索和跟随视为独立任务，仍缺乏用于系统评估的统一基准。为解决这些局限性，我们提出了统一具身搜索与跟随基准（UESF-Bench），这是一个大规模、多样化的具身人类搜索与跟随基准。该基准要求智能体处理语义引导的探索、可靠的行为切换与恢复以及延迟的身份确认。为此，我们提出了SeekFollow-VLA，一种视觉-语言-动作框架，具有任务驱动的路由机制，用于搜索和跟随之间的潜在阶段推断与转换建模。实验结果表明，SeekFollow-VLA在单人和多人环境中均优于单头基线和双头基线，为统一具身搜索与跟随建立了基线。

## Abstract
Language-guided human following is an important capability for embodied agents, but existing benchmarks typically assume that the target person is visible at the start of an episode. This setting simplifies the problem and overlooks a more realistic requirement: an agent often needs to first find a language-described target and then persistently follow that target in a dynamic environment. While recent work has started to study human search, existing settings are typically evaluated in task-specific scenarios and often rely on stronger prior knowledge of the environment. Moreover, they usually treat searching and following as separate tasks and still lack a unified benchmark for systematic evaluation. To address these limitations, we introduce the Unified Embodied Seeking and Following Benchmark (UESF-Bench), a large-scale and diverse benchmark for embodied human seeking and following. The benchmark requires agents to handle semantic-guided exploration, reliable behavior switching and recovery, and delayed identity grounding. To this end, we propose SeekFollow-VLA, a vision-language-action framework with a task-driven routing mechanism for latent phase inference and transition modeling between seeking and following. Experimental results show that SeekFollow-VLA achieves clear improvements over both single-head and dual-head baselines across single-person and multi-person environments, establishing a baseline for unified embodied seek-and-follow.

---

## 论文详细总结（自动生成）

# UESF-Bench: 统一具身搜索与跟随的基准测试与探究

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现有具身AI基准（如EVT-Bench）通常假设目标人物在任务开始时可见，这简化了问题，忽略了更现实的需求——机器人需要先根据语言描述搜索一个初始不可见的目标，然后持续跟随。虽然已有工作开始研究人类搜索，但通常将搜索和跟随视为独立任务，缺乏统一基准。
- **核心问题**：如何让具身智能体在单条融合指令下，自主完成从语义引导的目标搜索到动态跟随的连续过程，并处理行为切换、身份延迟确认等挑战。
- **整体含义**：提出UESF-Bench基准填补统一搜索-跟随评估的空白，并设计SeekFollow-VLA框架证明任务驱动路由机制的有效性。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将搜索与跟随统一为单一端到端任务，利用预训练VLM（Qwen3-4B）处理多模态观测，并通过**任务驱动路由机制**在隐式阶段边界下动态切换搜索与跟随行为。
- **关键技术细节**：
  - **视觉编码**：使用DINO-V3和SigLIP双塔编码器提取每个RGB帧的patch特征，通过GridPooling生成细粒度（64 tokens）和粗粒度（4 tokens）表示，历史帧用粗粒度、当前帧用细粒度以平衡效率与细节。
  - **动作头设计**：提出三种变体：
    1. **单头（SingleHead）**：一个共享的3层MLP直接预测10个未来航点。
    2. **双头+阶段感知路由器（DualHead-PA）**：分离的搜索头和跟随头，由三层MLP路由器输出软权重融合航点。
    3. **双头+任务驱动路由器（DualHead-TD）**：路由器额外输出任务logit，受阶段标签（搜索=0，跟随=1）监督，损失为航点MSE + 交叉熵任务损失（权重β_nav=10）。
- **公式与算法**：
  - 航点预测：$\hat{W}_T = \alpha_{\text{seek}} \hat{W}_{\text{seek}} + \alpha_{\text{follow}} \hat{W}_{\text{follow}}$
  - 损失函数：$\mathcal{L} = \beta_{\text{nav}} \mathcal{L}_{\text{wp}} + \mathcal{L}_{\text{stage}}$
  - 阶段标签定义：目标首次出现在1.7米内且智能体面对目标后，后续步骤标签为1（跟随），之前为0（搜索）。

## 3. 实验设计：数据集/场景、基准、对比方法

- **数据集与场景**：
  - **UESF-Bench**：基于Habitat 3.0模拟器，构建于777个HM3D和MP3D场景，包含4859个不同外观的虚拟角色，共15905个episodes（训练11310，验证1510，测试3085）。场景分为**单人场景**和**多人场景**（2-7个干扰者）。总计143万样本，每个样本包含RGB历史、目标描述、专家轨迹。
- **基准**：UESF-Bench本身作为新基准，提供语言引导的搜索+跟随统一任务。
- **对比方法**：三种动作头变体：
  - SingleHead（单头）
  - DualHead-PA（双头+阶段感知路由器）
  - DualHead-TD（双头+任务驱动路由器，即所提方法）
- **评价指标**：
  - TSR（任务成功率）
  - CR（碰撞率）
  - FR（跟随比率）
  - Search SPL（搜索成功路径长度加权效率）

## 4. 资源与算力

- **明确提及**：训练使用5块NVIDIA H100 GPU，采用分布式混合精度训练。
- **超参数**：初始学习率2e-5，batch size 22，航点预测范围10，历史窗口长度31，每1000步验证一次，最大训练epochs 10，实际最佳checkpoint在2-3个epoch内获得。
- **未说明**：单次训练总时长、数据加载等其他资源消耗未提及。

## 5. 实验数量与充分性

- **实验数量**：
  - 主实验（表1、表2）：在单人测试集和多人测试集分别对比三种变体，每组4个指标。
  - 消融分析：通过对比图（图4）展示了路由权重动态变化，定性说明行为切换。
  - 缺乏额外的消融实验（如改变历史长度、航点数量、不同VLM骨干等）。
- **充分性与公平性**：
  - 实验规模较大（143万样本、777场景），覆盖多难度场景。
  - 对比方法内保持骨干一致（Qwen3-4B），仅变化动作头，确保了公平性。
  - 但仅对比了自身变体，未与其他现有方法（如TrackVLA、Uni-NaVid等）在同一设置下比较，因为它们是仅跟随任务或不同设置，但论文未将此视为局限。
  - 多人与单人的唯一差异是加入干扰者，环境特征基本一致，变化可控。
- **偏差风险**：阶段标签定义（首次发现后一直为跟随）可能不够精细（例如目标短暂丢失后应回到搜索标签），可能引入标签噪声，但论文未讨论这一点。

## 6. 论文的主要结论与发现

- **主要结论**：SeekFollow-VLA（DualHead-TD）在单人和多人场景下均显著优于单头和普通双头基线：
  - 单人间：TSR 0.35 vs. 0.04/0.05；Search SPL 0.53 vs. 0.30/0.27；FR 0.92 vs. 0.85/0.85。
  - 多人群：TSR 0.20 vs. 0.04/0.04；Search SPL 0.55 vs. 0.31/0.26；FR 0.82（略低于DualHead-PA的0.85）。
- **关键发现**：任务驱动路由能够有效分离搜索与跟随行为，并在隐式阶段边界下实现可靠切换（图4显示明确的权重切换模式）。多人场景下性能下降更明显，说明身份混淆是核心挑战。

## 7. 优点：方法或实验设计上的亮点

- **问题定义创新**：首次将语言引导的搜索与跟随统一为一个连续任务，更贴近真实服务机器人场景。
- **基准构建**：UESF-Bench规模大（1.43M样本）、多样性高（777场景、4859角色、单/多人）、覆盖广，为后续研究提供标准化测试平台。
- **方法简洁有效**：在预训练VLM基础上只修改动作头设计，即取得显著提升，说明路由机制是关键。
- **路由动态可视化**：图4直观展示行为切换，增强了方案可信度。
- **损失设计合理**：航点回归+辅助阶段分类损失，既保证姿态精度又提供行为监督。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制

- **实验覆盖不足**：
  - 仅对比了自身变体，未与现有独立搜索方法（如MLLM-Search）或独立跟随方法（如TrackVLA）进行直接比较，无法凸显统一框架的绝对优势。
  - 缺乏对不同VLM骨干（如DeepSeek-V3、GLM）的鲁棒性测试。
  - 未进行超参数分析（如β_nav、历史长度、航点数量对性能的影响）。
- **偏差风险**：
  - 阶段标签定义过于简化：目标一旦被发现就永久标记为“跟随”，忽略了目标丢失后需重新搜索的实际情况，这可能误导路由器在丢失后仍保持跟随权重。论文虽提及“恢复”挑战但未在标签中体现。
  - 多人场景下干扰者行为随机，未控制干扰程度（如数量、运动模式），可能导致结果方差大。
- **应用限制**：
  - 所有实验在仿真环境（Habitat 3.0）中进行，尚未在真实机器人上验证，仿真与现实的差距（如光照、纹理、动态物体）未知。
  - 假设目标始终在环境中且可被相机观测，未考虑目标完全离开场景或附近无人的情况。
  - 动作空间为离散航点，依赖模拟器导航堆栈，真实机器人需适配连续控制。
- **缺少消融**：未消融视觉编码器（能否只用单塔？）、网格池化层级、双头是否必要（单头+任务监督？）等。

（完）
