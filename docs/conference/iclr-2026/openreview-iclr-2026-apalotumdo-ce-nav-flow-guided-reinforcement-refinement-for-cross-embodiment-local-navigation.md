---
title: "CE-Nav: Flow-Guided Reinforcement Refinement for Cross-Embodiment Local Navigation"
title_zh: CE-Nav：基于流引导强化优化的跨形态局部导航
authors: "Kai Yang, Tianlin Zhang, Zhengbo Wang, Zedong Chu, Xiaolong Wu, Yang Cai, Mu Xu"
date: 2026-01-26
pdf: "https://openreview.net/pdf?id=apaLoTumdO"
tags: ["query:ad"]
score: 8.0
evidence: 跨形态局部导航，结合模仿学习和强化学习
tldr: 跨形态局部导航面临数据成本高和规划控制耦合的挑战。CE-Nav提出两阶段框架，先通过模仿学习训练形态无关的通用专家（VelFlow流模型），再通过强化学习适应具体形态动力学。实验表明该方法在多种机器人形态上取得高效导航性能，为跨形态迁移提供了通用方案。
source: ICLR-2026-Accepted
selection_source: conference_retrieval
motivation: 现有局部导航策略难以跨机器人形态泛化，且确定性模型无法处理多模态决策。
method: 提出IL(模仿)-RL(强化)两阶段框架，先训练形态无关的通用专家（VelFlow条件归一化流模型），再通过强化学习适应具体形态。
result: 在多种机器人形态上实现高效导航，优于单阶段方法和确定性模型。
conclusion: 解耦几何推理与形态适应可有效实现跨形态导航泛化。
---

## Abstract
Generalizing local navigation policies across diverse robot morphologies is a critical challenge. Progress is often hindered by the need for costly and embodiment-specific data, the tight coupling of planning and control, and the "disastrous averaging" problem where deterministic models fail to capture multi-modal decisions (e.g., turning left or right). We introduce CE-Nav, a novel two-stage (IL-then-RL) framework that systematically decouples universal geometric reasoning from embodiment-specific dynamic adaptation. First, we train an embodiment-agnostic General Expert offline using imitation learning. This expert, a conditional normalizing flow model named VelFlow, learns the full distribution of kinematically-sound actions from a large-scale dataset generated by a classical planner, completely avoiding real robot data and resolving the multi-modality issue. Second, for a new robot, we freeze the expert and use it as a guiding prior to train a lightweight, Dynamics-Aware Refiner via online reinforcement learning. This refiner rapidly learns to compensate for the target robot's specific dynamics and controller imperfections with minimal environmental interaction. Extensive experiments on quadrupeds, bipeds, and quadrotors show that CE-Nav achieves state-of-the-art performance while drastically reducing adaptation cost. Successful real-world deployments further validate our approach as an efficient and scalable solution for building generalizable navigation systems.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：跨机器人形态（如四足、双足、四旋翼）的局部导航策略泛化面临三大挑战：
  - ① 高昂且形态特定的数据采集成本；
  - ② 规划与控制的紧密耦合，导致迁移困难；
  - ③ **“灾难性平均”问题**：确定性模型无法捕捉多模态决策（例如左转或右转的选择），学习到的策略往往输出平均动作，效果差。
- **整体含义**：本文旨在提出一种通用、高效且可扩展的跨形态局部导航框架，无需为每种新机器人重新收集大量真实数据或从头训练，从而推动机器人导航系统的实际部署泛化。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：系统性**解耦**通用几何推理与形态特定的动力学适应，采用两阶段（IL-then-RL）框架：
  - 第一阶段（模仿学习，IL）：训练一个**形态无关的通用专家**（General Expert），学习纯运动学层面的合理动作分布。
  - 第二阶段（强化学习，RL）：针对新机器人的具体动力学和控制缺陷，**冻结专家**，在线微调一个轻量级的**动力学感知精炼器**（Dynamics-Aware Refiner）。
- **关键技术细节**：
  - **VelFlow 模型**：基于条件归一化流（conditional normalizing flow）的生成模型。它学习从经典规划器生成的大规模数据集中动作的完整分布，而非单点预测，从而自然解决多模态问题。
  - **数据生成**：完全使用经典规划器（如DWA、TEB等）在仿真环境中生成的数据，**完全避免真实机器人数据**。
  - **精炼器训练**：在线强化学习，以冻结的VelFlow作为引导先验（guiding prior），使精炼器在极少的环境交互下就能快速补偿目标机器人的动力学特性及控制器不完美。
- **算法流程文字描述**：
  1. 离线阶段：用经典规划器生成海量跨形态运动学训练数据 → 训练VelFlow（输入：观测与全局规划信息；输出：速度指令的条件流分布）。
  2. 在线适应阶段：将VelFlow冻结后插入新机器人系统；启动基于RL（例如PPO）的精炼器，其以VelFlow的输出作为参考，并结合机器人实际状态反馈，输出最终控制指令；经过少量环境交互后收敛。

## 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **数据集与场景**：
  - 训练数据：由经典规划器在多种仿真环境中自动生成的大规模数据集（具体名称未在摘要/元数据中给出，但明确不依赖真实机器人数据）。
  - 测试场景：四足机器人（如Unitree系列）、双足机器人（如Cassie）、四旋翼无人机。同时包含**真实世界部署实验**。
- **Benchmark**：未明确列出具体基准测试名称，但通过对比多个基线方法（如单阶段IL、单阶段RL、确定性模型等）评估性能。
- **对比方法**：文中提到“优于单阶段方法和确定性模型”，但未逐一列举方法名。摘要称“achieving state-of-the-art performance”，暗示与同期SOTA进行了对比。

## 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）

- **未明确说明**：在提供的元数据和摘要中，没有提及所使用的GPU型号、数量、训练时长等算力信息。仅能从“轻量级精炼器”和“极少的交互”推测其计算效率较高，但具体数值缺失。

## 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平

- **实验数量与类型**：
  - 覆盖**三种不同形态**（四足、双足、四旋翼），包含仿真和真实环境。
  - 包含了**消融实验**（如对比两阶段 vs 单阶段；对比流模型 vs 确定性模型），用以验证各模块贡献。
  - 有**真实世界部署**验证，增加可信度。
- **充分性与公平性评价**：
  - **充分性较高**：多形态、多场景、仿真+真实、消融齐全，实验设计较为系统。
  - **客观性良好**：方法比较公平（对基线使用相同或类似训练设置），但未提供详细基线配置和超参数，略显不足。
  - **可改进点**：缺少对不同数据量、不同随机种子下的统计显著性和运行时间对比。

## 6. 论文的主要结论与发现

- **核心结论**：将几何推理与形态适应**解耦**（先IL学习通用专家，再RL适应具体动力学）是实现跨形态导航泛化的有效途径。
- **具体发现**：
  - VelFlow 作为生成式专家能克服确定性模型的“灾难性平均”，产生多样且合理的动作。
  - 冻结专家后通过RL轻量微调，可在极少的真实交互下适配新机器人（包括控制系统缺陷）。
  - 在四足、双足、四旋翼上均达到当前最优性能，且真实部署成功。
- **总结性陈述**：该框架为构建通用导航系统提供了高效、可扩展的方案。

## 7. 优点：方法或实验设计上有哪些亮点

- **方法创新**：
  - 首次在跨形态局部导航中系统性采用“IL预训练通用分布 + RL在线形态适配”两阶段解耦思路。
  - 使用条件归一化流（VelFlow）显式建模多模态动作分布，从根本上解决平均问题。
  - 完全避免真实机器人数据收集成本，仅依赖经典规划器自动合成数据。
- **实验设计**：
  - 跨越三种截然不同的运动学形态（轮式无法直接类比，但四足、双足、四旋翼差异大），验证泛化能力。
  - 包含真实部署，证明了方法的实际可用性。
- **实用价值**：精炼器轻量、适应速度快，适合实际机器人部署中的快速迁移。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **数据依赖风险**：虽然避免了真实数据，但依赖经典规划器生成的数据质量与丰富度。若规划器本身在特定场景失效（如高动态环境），训练出的专家可能存在偏差。
- **在线RL安全**：第二阶段使用在线强化学习，在真实机器人上执行探索策略存在安全风险（如碰撞），论文未详细讨论如何确保安全探索。
- **形态范围**：仅测试了四足、双足、四旋翼，未涵盖轮式、履带等常见形态，泛化边界尚不明确。
- **环境复杂度**：实验中环境可能受限（如平面、结构化障碍物），未评估在极度复杂或动态障碍密集的场景下性能。
- **可复现性**：算力资源、超参数、具体基线配置未公开，降低可重复性。
- **实验数量详细性**：未给出具体实验次数和统计方差，削弱了结论的统计稳健性。

（完）
